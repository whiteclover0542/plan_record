# T07 플랜두씨 다이어리 2 — 제출 문서

> 작업 근거(curl 요청/응답 전문, 설계 결정, SQL 마이그레이션 과정)는 [PROGRESS.md](PROGRESS.md),
> 5일 실사용 로그 원본은 [card5-log.md](card5-log.md) 참고.
> T06(로그인 없는 버전) 최종 제출 문서는 [archive/t06/SUBMISSION.md](archive/t06/SUBMISSION.md).

## 결과물 주소

https://whiteclover0542.github.io/plan_record/

첫 화면은 로그인 화면이다. 로그인 없이는 자료 화면이 열리지 않고, 계정을 만들지 않아도 로그인 화면까지는 누구나 열린다.

## 소스 주소

https://github.com/whiteclover0542/plan_record/tree/6b6673b

브랜치가 아니라 제출 시점의 고정 commit을 가리킨다 — 이후 커밋에 영향받지 않고 제출 당시 소스 상태를 그대로 볼 수 있도록.

이어서 사용한 T06 최종 결과: [archive/t06/](archive/t06/) — T06 소스 이력이 이 브랜치의 조상 커밋으로 포함되어 있다(T07-C77/C78).

---

## 인증 구현 설명서

① **무엇으로 붙였나** — Supabase Auth(이메일+비밀번호), `@supabase/supabase-js@2.112.4`.

② **왜 그걸 골랐나** — 이미 Supabase Postgres + Row Level Security 위에서 이 앱이 동작 중이라 `auth.uid()`로 바로 연결됨. 검토했으나 고르지 않은 방법:
- 직접 구현(bcrypt+JWT) — 세션 무효화·토큰 재발급 등 보안 디테일을 직접 구현·검증하는 부담이 큼
- 별도 인증 서비스(Auth0/Clerk) — 인프라가 하나 더 늘고 DB RLS와의 연동 지점이 늘어남

③ **어디를 어떻게 고쳤나** — 가입·로그인·로그아웃·자료 조회 네 흐름:
- **가입**: `index.html`의 `#signup-form` → `pds-auth.js`(`signupForm` submit 핸들러) → `pds-app.js`(`Auth.signUp`, `sb.auth.signUp`) → Supabase Auth REST `POST /auth/v1/signup` → `auth.users`에 계정 생성(비밀번호는 bcrypt 해시로 저장)
- **로그인**: `#login-form` → `pds-auth.js`(`loginForm` submit) → `pds-app.js`(`Auth.signIn`, `sb.auth.signInWithPassword`) → `POST /auth/v1/token?grant_type=password` → JWT access/refresh token 발급
- **로그아웃**: `#logout-btn` → `pds-auth.js` → `pds-app.js`(`Auth.signOut`) → `POST /auth/v1/logout` → 세션 사라짐을 감지해 화면이 로그인 화면으로 복귀
- **자료 조회**: 로그인 성공 후 `pds-ui.js`의 `refreshPlans()` → `pds-app.js`(`Plans.list`) → PostgREST가 요청 JWT의 `auth.uid()`로 `supabase/schema.sql`의 `plans_select` 정책(`using (user_id = auth.uid())`)을 적용해 내 자료만 반환

DB 쪽: `supabase/migration-t07-auth.sql`로 기존 T06 데이터를 보존한 채 5개 표(`plans`/`plan_revisions`/`todos`/`execution_logs`/`retrospective_notes`) 전부에 `user_id`(auth.users 참조) 컬럼을 추가하고, RLS를 `authenticated` + `user_id = auth.uid()` 기준으로 재작성했다. `anon` 롤에는 이 5개 표 어떤 권한도 남기지 않았다.

④ **안 열리는 것을 확인한 기록** — anon key만으로 라이브 프로젝트에 curl로 직접 요청을 보내 확인(disposable 테스트 계정 사용, 비밀번호는 어디에도 남기지 않음). 아래 값은 실제 관측된 상태 코드다.

| 확인 | 성공 요청 | 거절/무효 요청 |
|---|---|---|
| 가입 중복 | 새 이메일 `POST /auth/v1/signup` → `200` | 같은 이메일 재가입 → `422 user_already_exists` |
| 로그인 문구 | 올바른 비밀번호 `POST /auth/v1/token?grant_type=password` → `200` | 틀린 비밀번호/존재 안 하는 이메일 → 둘 다 `400 {"error_code":"invalid_credentials","msg":"Invalid login credentials"}`(동일 문구) |
| 비밀번호 저장 | (해당 없음) | SQL Editor 조회: `encrypted_password` = `$2a$10$...`(bcrypt, 입력 글자와 무관) — 같은 비밀번호로 만든 두 계정도 서로 다른 값 |
| 세션 조회 | 로그인 상태로 `GET /rest/v1/plans` → `200` | 로그아웃 뒤 refresh token 재사용 → `400 refresh_token_not_found`(즉시 무효화) |
| 계정 간 읽기/수정/삭제 | 본인 자료 조회/수정 → `200`(정상 반영) | 남의 `plans`/`todos` 행에 대한 읽기·수정·삭제 시도(양방향) → `200`+빈 배열(값 변경 없음, 실제 대조로 확인), 소유권 위조·교차 INSERT 시도는 `403` |
| 비로그인 직접 요청 | (해당 없음) | `Authorization` 헤더 없이 `GET /rest/v1/plans` → `401 permission denied for table plans` |
| 계정 삭제 | `POST /rest/v1/rpc/delete_my_account` → `204` | 삭제 후 같은 계정으로 재로그인 → `400 invalid_credentials`, 자료도 FK cascade로 실제 삭제 |

⑤ **AI와 나** — 인증 방식 선택은 AI가 Supabase Auth를 추천했고 내가 최종 승인했다. RLS 마이그레이션 SQL과 로그인 게이트 코드는 AI가 작성했지만, 대시보드에서 단계별로 실제 실행한 것과 실계정 생성·데이터 이관 확인은 내가 직접 했다. 카드2~4의 거절 근거는 AI가 라이브 프로젝트에 curl로 직접 요청을 보내 모았고, 예상과 다른 관측(로그아웃 후에도 access token이 통함, 남의 행 수정이 403이 아니라 빈 배열)이 나왔을 때 문장을 고쳐 덮는 대신 원인을 SQL로 규명해 ⑥에 그대로 남기게 했다. 5일 실사용 로그의 질문·지표·계산 규칙은 내가 정했고 기록도 내가 직접 입력했으며, 진행 중 AI의 안내가 전제부터 틀렸을 때는 바로잡았다(아래 3줄 ③).

⑥ **아직 못 막은 것**
- 로그아웃·비밀번호 변경·계정 삭제 중 어느 것을 해도, 그 전에 이미 발급된 access token(JWT)은 자연 만료(최대 1시간) 전까지 계속 통한다. refresh token이나 계정 자체는 즉시 무효화되지만, access token은 서명·만료만으로 자체 검증되는 무상태 토큰이라 서버가 개별 취소를 기억하지 않기 때문이다. 토큰이 탈취됐을 때 최대 1시간의 위험 구간이 남는다.
- 남의 계획을 고치거나 지우려는 시도가 항상 명시적 403은 아니고, 경우에 따라 "조용한 무효 처리"(200+빈 배열)로 응답한다(원인: Postgres RLS가 UPDATE/DELETE 대상 행을 찾을 때도 SELECT 정책을 함께 적용하기 때문 — 데이터는 안전하지만 응답 형태가 균일하지 않다).
- 무차별 대입(brute-force) 로그인 시도 횟수 제한을 이 앱이 직접 만들지 않았다 — Supabase Auth 자체의 기본 rate limit에 의존한다.

---

## 짧은 확인 방법

① 어디로 가나요 — https://whiteclover0542.github.io/plan_record/ 접속(첫 화면이 로그인/가입 화면).
② 세 단계 안에 무엇을 하나요 — (1) 가입하기로 새 계정을 만들거나 기존 계정으로 로그인 (2) "1. 계획" 탭에서 계획 목록을 본다 (3) "3. 돌아보기" 탭에서 집계 숫자를 확인한다.
③ 무엇이 보이면 통과인가요 — 로그인 전에는 로그인 화면만 보이고, 로그인 후에는 그 계정에 속한 자료만 보인다(다른 계정 자료는 절대 섞이지 않는다).
④ 안 될 때는 무엇이 보이나요 — 로그인 없이 자료 화면 주소를 열면 계속 로그인 화면만 보이고, 개발자 콘솔에는 Supabase REST 요청이 401/403으로 거절된 로그가 찍힌다.

## AI와 내 판단 3줄

① AI에게 맡긴 일 — RLS·소유권 마이그레이션 SQL 설계·작성, 로그인 게이트 화면·코드 구현, anon key로 라이브 프로젝트에 curl 테스트를 직접 돌려 근거(성공/거절 요청·응답) 수집, 발견한 문제(토큰 잔존, 조용한 무효 처리)의 원인을 SQL로 규명, 문서 정리.

② 내가 직접 판단한 일 — 인증 방식(Supabase Auth) 최종 승인, 이메일 확인 정책 대시보드 설정, SQL 마이그레이션을 대시보드에서 직접 단계별 실행(실제 계정 생성 포함), 5일 로그의 1일차 관찰 포인트 제시, 계획 삭제 기능 필요성 판단, 실제 계획·할 일·실행 기록 입력.

③ AI 제안을 따르지 않은 일 — 5일차(2026-09-05)에 세 번 있었다. (1) AI가 "지표가 나오려면 할 일을 완료 처리해야 한다"는 전제로 안내하길래, 1일차에 고정해 둔 결측 규칙("미완료는 차이 계산에서 자동 제외")대로면 미완료로 남기는 것도 정당한 기록이라고 지적해 전제를 바로잡았다 — 지표를 예쁘게 만들려고 실사용을 왜곡하면 과제 취지가 무너진다. (2) 실행기록이 60분·64분 두 건으로 중복됐을 때 AI는 옛 기록을 SQL로 지워 화면 합계와 손 계산을 일치시키자고 권했지만, **기록을 고쳐 숫자를 맞추는 대신 1일차에 미리 정한 중복 규칙(최근 1건만 사용)을 그대로 적용하고 화면과의 60분 차이를 근거로 남기는 쪽**을 택했다. (3) 화면에 남은 `test 1`/`test 2` 계획을 지우자는 제안은 받아들이지 않고 실제 사용 흔적 그대로 두었다.

---

## 카드별 증거

상세 근거는 [PROGRESS.md](PROGRESS.md)의 "마무리 체크리스트"·"curl 테스트 결과 요약"에 요청/응답 단위로 정리돼 있다.

### 카드 1 — 무엇으로 붙일지 고르고, 이유를 적기

Supabase Auth(이메일+비밀번호), `@supabase/supabase-js@2.112.4`. 검토했으나 고르지 않은 방법과 이유는 위 설명서 ②.

- 가입 → `POST /auth/v1/signup` `200` + 세션 즉시 발급(이 프로젝트는 이메일 확인 꺼짐)
- 로그인 → `POST /auth/v1/token?grant_type=password` `200`
- 로그아웃 → `POST /auth/v1/logout` `204`
- 로그인 없이 자료 화면 주소를 열면 → 로그인 화면만 보임. 서버 쪽에서도 `401 permission denied for table plans`
- 같은 이메일 재가입 → `422 user_already_exists`
- 비밀번호 틀림 / 없는 이메일 → **둘 다 완전히 같은 문구** `400 Invalid login credentials`
- T06 자료 이관: 마이그레이션 후 `still_null=0`(계획 5건 전부), 로그인 후 화면에서 T06 계획이 그대로 보임
- 배포 사이트에서 UI 클릭으로 가입→자동로그인→로그아웃→재로그인→셀프 계정삭제까지 14개 항목 재확인(2026-09-03)

### 카드 2 — 비밀번호를 어떻게 맡아 두는지 보이기

비밀번호 저장은 전부 Supabase Auth(GoTrue)에 위임했고, 앱 코드는 비밀번호를 직접 해싱하거나 저장하지 않는다.

- 실제 저장 값(`auth.users.encrypted_password`)을 SQL Editor로 조회 → `$2a$10$...` → **bcrypt(cost 10)** 확정. 입력한 글자는 어디에도 보이지 않는다.
- **같은 비밀번호로 만든 두 계정(X/Y)의 저장 값이 서로 다름**을 대조로 확인(`$2a$10$2kJUjB6X...` vs `$2a$10$k3LicXws...`) — salt가 달라서다.
- 수집한 모든 요청/응답 파일을 실제 비밀번호 문자열로 grep해 원문이 전무함을 확인했다.

### 카드 3 — 들어온 사람을 어떻게 기억하는지 보이기

JWT access token(`alg: ES256`, 비대칭키) + refresh token 방식.

- 만료: `expires_in = 3600`(1시간). JWT payload의 `exp`/`iat`를 직접 디코드해 확인.
- 토큰은 URL에 실리지 않고 `Authorization` 헤더로만 전달된다.
- 같은 주소·같은 방식(`GET /rest/v1/plans?id=eq...`)으로 로그아웃 전/후를 비교: **로그아웃 직후 refresh token 재사용 → `400 refresh_token_not_found`(즉시 무효화)**. 다만 access token 자체는 만료 전까지 통한다 → 설명서 ⑥에 그대로 적었다.
- 서명용 비밀키는 서버에만 있다. 이 프로젝트는 `ES256`(비대칭키)이라 애초에 공유 비밀키 자체가 없는 구조이며, 저장소 전체 grep으로 클라이언트 코드·배포 파일·Git 이력에 없음을 확인했다.

### 카드 4 — 남의 자료가 안 열리는 것을 보이기

계정 두 개(A/B, 이후 P/Q로 재현)를 만들고 각각 자료를 넣은 뒤 양방향으로 교차 접근을 시도했다. 비밀번호는 이 문서 어디에도 적지 않았다.

| 시도 | 결과 |
|---|---|
| A→B, B→A 읽기·수정·삭제 (양방향) | `200` + **빈 배열**, 상대 자료의 제목·건수 실제 대조로 변경 없음 확인 |
| PATCH 본문에 남의 `user_id`를 넣어 소유권 훔치기 | 무효(빈 배열) |
| 내 행의 `user_id`를 남에게 넘기기 | **`403` `42501` new row violates row-level security policy** |
| 남의 계획 id로 할 일 끼워 넣기 (교차 INSERT) | **`403` `42501`** |
| `GET /rest/v1/plans?user_id=eq.<남의 uuid>` (주소 위조) | `200` + 빈 배열 — RLS가 클라이언트 필터와 무관하게 서버에서 강제 |
| `complete_todo` RPC로 남의 할 일 완료 처리 | `200`이지만 `newly_completed: false`(실제로 완료 안 됨) |
| `Authorization` 없이 직접 요청 | **`401 permission denied for table plans`** — anon 롤에 권한 자체가 없음 |
| 목록 조회 | 본인 것만 반환, 남의 자료 섞이지 않음 |

거절을 만드는 소스: `supabase/migration-t07-auth.sql` STEP 6(RLS 정책)·STEP 8(RPC 소유자 조건), `supabase/schema.sql` 동일 섹션.

읽기·수정·삭제가 `403`이 아니라 `200`+빈 배열로 막히는 이유까지 SQL로 규명해 설명서 ⑥에 적었다 — 응답 코드를 설계 의도대로 꾸미지 않고 실제 관측값을 그대로 남겼다.

### 카드 5 — 설명서로 묶고, 잠근 앱으로 5일 써 보기

로그인이 걸린 앱을 실제 내 계정으로 **서로 다른 실제 날짜 5일** 동안 사용했다. 원본 기록은 [card5-log.md](card5-log.md).

**1일차에 먼저 고정한 것** — 질문: "계획한 우선순위·시간 배분대로 하루 할 일을 빠짐없이 소화하고 있는가?" / 지표: 그날 완료한 할 일의 **일일 차이 시간(실제−예상) 합계** / 단위: 분 / 계산 규칙: 결측(미완료)은 제외, 중복(실행기록 2건 이상)은 최근 1건만, 이상치(실제가 예상의 5배 이상이거나 음수)는 제외하고 메모, 반올림은 분 단위 정수, 주 시작은 월요일.

**계획 규칙 변경** — 2026-09-02 18:09(Asia/Seoul), **Day2 기록 직후·Day3 시작 전**. 바꾸기 전: 예상 시간을 감으로 적는다 → 바꾼 뒤: 비슷한 예전 할 일의 실제 걸린 시간을 먼저 찾아보고 참고해서 적는다. 이유: Day1~2의 오차가 작긴 했지만 감이 우연히 맞은 것인지 구분되지 않아, 산정 방식 자체를 바꿔 비교해 보려고.

| 일자 | 날짜 | 완료 | 예상(분) | 실제(분) | 일일 차이(분) | 규칙 |
|---|---|---|---|---|---|---|
| Day 1 | 2026-09-01 | 1 | 60 | 55 | **-5** | 변경 전(감으로) |
| Day 2 | 2026-09-02 | 2 | 130 | 128 | **-2** | 변경 전(감으로) |
| *규칙 변경* | *2026-09-02 18:09* | | | | | *감 → 과거 실제 기록 참고* |
| Day 3 | 2026-09-03 | 1 | 120 | 90 | **-30** | 변경 후(적용 안 함) |
| Day 4 | 2026-09-04 | 1 | 90 | 105 | **+15** | 변경 후(적용함) |
| Day 5 | 2026-09-05 | 1 | 60 | 64 | **+4** | 변경 후(적용함) |
| **합계** | | **6** | **460** | **442** | **-18** | |

전후 비교는 같은 지표·같은 단위·같은 계산 규칙으로만 했다. 절대 오차 평균은 변경 전 **3.50분** → 변경 후 **16.33분**(새 규칙을 실제로 적용한 Day4~5만 보면 9.50분)으로 **줄지 않고 오히려 커졌다**. 다만 규칙을 바꾼 시점과 작업 유형이 반복형 집안일에서 개발 작업으로 바뀐 시점이 겹쳐 있어, 이 5일만으로는 규칙 효과와 작업 유형 효과를 분리할 수 없다 — 결론을 규칙 덕분이라고 포장하지 않고 여기까지만 적는다.

**화면 합계와 손 계산 대조** — 예상 합계는 화면·손 계산 모두 460분으로 일치. 실제 합계는 화면 502분 vs 손 계산 442분으로 **정확히 60분 어긋나며, 원인을 규명했다**: Day5 할 일에 실행기록이 2건(60분·64분) 쌓였는데 화면은 실행기록을 전부 더하고, 지표는 1일차에 고정한 중복 규칙대로 최근 1건(64분)만 쓰기 때문이다. 차액 60분이 중복된 옛 기록 1건과 정확히 일치한다. `execution_logs`에는 DELETE 정책이 없어(append-only) 옛 기록을 지워 화면을 맞출 수 없고, 그렇게 하는 대신 **미리 정해 둔 규칙을 지키고 차이를 근거로 남겼다.**

**내보내기** — "전체 자료 내보내기 (JSON)"로 plans·plan_revisions·todos·execution_logs·retrospective_notes 5개 표를 파일 하나(`pds-export-<날짜>.json`)로 받는다. 쿼리에 `user_id` 필터가 없고 RLS가 서버에서 강제하므로 내 자료만 담긴다.

**계정 삭제** — `delete_my_account()` `204` → 재로그인 `400 invalid_credentials`, 자료도 FK cascade로 실제 삭제됨을 테스트 계정으로 확인.

---

## 제출물 확인

- [x] 가입·로그인·로그아웃이 되고, 로그인 없이 자료 화면을 열면 로그인 화면이 나옵니다.
- [x] 저장된 비밀번호에 입력한 글자가 보이지 않습니다.
- [x] 로그아웃 뒤 같은 값으로 다시 요청하면 거절됩니다. *(refresh token은 즉시 거절. access token은 만료 전까지 통하며, 이 한계를 설명서 ⑥에 적었습니다.)*
- [x] 남의 자료를 읽기·수정·삭제하려는 요청이 양방향으로 모두 거절되고, 목록에도 섞이지 않습니다.
- [x] 설명서 여섯 항목이 나뉘어 있고, ⑥이 비어 있지 않습니다.
- [x] 서로 다른 날짜 5일의 기록과 3일차 전 규칙 변경이 순서대로 있습니다.
- [x] 제출물에 비밀번호·토큰·비밀키 원문이 없습니다. *(저장소 전체 grep으로 확인 — `config.js`의 anon key 하나뿐이며, 이는 Supabase 설계상 공개되는 키입니다.)*
