# T06 플랜두씨 다이어리 1 — 진행 관리

> 요구사항 원문은 [ASSINGMENT.md](ASSINGMENT.md)에 동결되어 있다. 이 문서는 진행 상황을 계속 갱신하는 관리 문서다.
> 2026-08-31: 과제 요구사항이 습관 기록기(카드1~5, localStorage)에서 계획(Plan)-할일(Todo)-실행기록(Do)-돌아보기(See, 서버 DB)로 바뀌었다. **옛 습관 기록기는 지우지 않고 그대로 남긴다** — 화면 위 모드 전환 버튼("계획 다이어리" / "습관 기록기")으로 두 기능 모두 접근 가능하다. 옛 카드1~5의 진행 이력(설계 결정·마무리 체크리스트 등)은 git 이력에 보존되어 있고, 이 문서는 그 위에 새 카드(Plan-Do-See)의 진행 상황을 추가로 쌓는다.

## 진행 상태

| # | 작업 | 상태 | 비고 |
|---|------|------|------|
| 1 | 새 요구사항으로 `ASSINGMENT.md` 교체 | ✅ | 이전 내용은 git 이력에 보존 |
| 2 | 아키텍처 결정: Supabase(Postgres) + RLS + 정적 프론트(GitHub Pages 유지) | ✅ | 서버 코드 없이 supabase-js로 직접 REST 호출 |
| 3 | DB 스키마 설계·작성 (`supabase/schema.sql`) | ✅ | plans/plan_revisions/todos/execution_logs/retrospective_notes + `complete_todo`/`reopen_todo` RPC |
| 4 | 스키마 계약 파일 (`contracts/pds-schema-v2.json`) | ✅ | 표·컬럼·관계·시간대 규칙 요약 |
| 5 | Supabase 프로젝트 생성, 스키마 적용, 연결 확인 | ✅ | REST로 4개 테이블 200 응답, insert/select 권한 확인 |
| 6 | `config.js`(anon key 연결 정보) | ✅ | anon key는 공개 키 — service_role 키는 어디에도 사용 안 함 |
| 7 | 데이터 계층 (`pds-app.js`: CRUD/RPC/집계/정렬/내보내기) | ✅ | |
| 8 | 화면 구현 (`index.html`/`pds-ui.js`/`style.css` 추가분) | ✅ | 계획/할일/돌아보기 3탭 |
| 9 | 옛 습관 기록기 자료 정리 시도 → **되돌림** | ✅ | 처음엔 삭제했으나(테마·기능 유지 요청으로) git에서 전부 복구, 새 기능과 병합 |
| 10 | 자동화 테스트(headless Chrome + CDP)로 Plan-Do-See 핵심 플로우 검증 | ✅ | 15개 항목 전부 통과(1차 14/15 → 트리거 패치 후 재검증 3개 항목 모두 통과) |
| 11 | `plan_revisions` 트리거 권한 버그 수정 | ✅ | `security definer`로 패치 적용·재검증 완료. 연속 수정 2회 → 이력 2건 정확히 저장 확인 |
| 12 | 자동화 테스트용 합성 데이터(`__ui_test__`, `__revtest__`) 정리 | ✅ | SQL로 삭제 완료 |
| 13 | 옛 습관 기록기 + 새 Plan-Do-See를 한 페이지로 병합 | ✅ | 전역 변수·id 충돌 감사 → `export-btn` id 1건만 충돌 발견, `pds-` 접두사로 분리. 새 JS는 `pds-app.js`/`pds-ui.js`로 분리해 IIFE로 감싸 전역 오염 차단 |
| 14 | 병합 후 테마 통일 (다크 테마·폰트·컴포넌트 스타일 재사용) | ✅ | 새 화면 CSS를 옛 `:root` 색상 변수(`--bg`/`--panel`/`--accent-bg` 등)로 다시 작성, 별도 팔레트 안 씀 |
| 15 | 병합 후 통합 자동 테스트(모드 전환 + 옛 기능 + 새 기능 동시 검증) | ✅ | 15개 항목 전부 통과, 콘솔 에러 0건 — 습관 기록기(합성 기록 추가·캘린더 렌더·전체 삭제)와 Plan-Do-See(계획 생성·내보내기)가 한 페이지에서 서로 침범 없이 동작 확인 |
| 16 | 병합 테스트용 합성 데이터(`__merge_test__`) 정리 | ✅ | SQL로 삭제 완료 |
| 16b | 할 일 캘린더를 상단 고정 + 계획 기간을 이어진 막대로 표시 | ✅ | 목록 대신 월간 캘린더로 교체, 계획 기간이 날짜 칸 안에서 이어진 막대로 표시(레인 배정으로 겹치는 계획도 줄 안 겹침), 계획별 색상 지정(테두리·글자) 기능 추가(`color_border`/`color_text` 컬럼) |
| 16c | 카드2~4 남은 미검증 항목(C10/C19/C27/C32/C33) 값 단위로 재검증 | ✅ | 할 일 수정 5개 필드, 태그·상태 필터, 완료 처리 전후 계획값 불변, 예상/실제/차이 정확한 숫자(60/50/-10), 다음 계획 메모 이어받기(DB 링크 확인)까지 전부 자동 테스트로 확인 |
| 17 | GitHub에 커밋·푸시, GitHub Pages 배포 확인 | ✅ | `main`이 `origin/main`과 동기화된 상태 확인, 배포 주소(`https://whiteclover0542.github.io/plan_record/`)를 실제로 불러와 계획 다이어리 화면·로그인 없음 안내 문구 노출 확인(2026-08-31) |
| 18 | 내가 실제로 세운 계획 1개 이상 입력 (합성 데이터 아님, 카드5) | ✅ | "T06 플랜두씨 다이어리 앱 만들기" — 이 앱을 만드는 실제 작업 자체를 계획으로 등록(2026-08-27~09-07). git 커밋 이력의 실제 시각을 근거로 삼음 |
| 19 | 그 계획에 딸린 실제 할 일 5개 이상 입력 | ✅ | 실제 작업 단위로 할 일 6개 등록(습관 기록기 구현/캘린더·테마/PDS 스키마/PDS 화면·병합/배포·README/실사용 데이터 입력, 5개 완료·1개 진행중) |
| 20 | 실제 실행 기록(Do) 3개 이상 입력 | ✅ | `complete_todo` RPC로 5건 등록, 시작·종료 시각은 실제 git 커밋 시각 기준(Asia/Seoul). 병합 작업 건에는 실제 있었던 일(파일 실수 삭제→복구)을 막힌 이유로 기록 |
| 21 | 돌아보기 화면이 실제 자료로 채워지고 집계가 0이 아님 확인 | ✅ | REST로 직접 집계 확인: 계획수 6·완료 5·지연 0·막힘 1·예상 570분·실제 324분·차이 -246분(2026-08-31) |
| 22 | 배포된 화면에서 스크립트 모양 글자 재확인 | ⬜ | 로컬 자동 테스트로는 확인됨(#10) |
| 23 | 비밀값 노출 점검(브라우저 코드·콘솔·Git 기록) | ✅ | `config.js`에 anon key 하나만 있음을 재확인, JWT payload 디코드로 `"role":"anon"` 확인, `git log --all -p`로 전체 이력에 `service_role`/`secret_key`/비밀번호 패턴 없음 확인(문서 내 언급뿐)(2026-08-31) |
| 24 | 시크릿 창에서 로그인 없이 전체 플로우 재현 | ⬜ | |
| 25 | `README.md` 재작성(습관 기록기 + Plan-Do-See 병합 반영) | ✅ | 계획 다이어리/투두 기록기 두 기능을 각각 소개하도록 재작성(2026-08-31) |
| 26 | 검증 안내서 4줄 작성 | ✅ | 실제 배포 화면·실데이터 기준으로 작성(2026-08-31) |
| 27 | AI 3줄 작성 | ✅ | 초안 작성(2026-08-31) — 본인 목소리로 맞는지 검토 필요 |
| 28 | 최종 제출 문서(SUBMISSION.md/HTML/PDF) 작성 | ⬜ | 앱 완성 뒤 별도 단계로 진행 |

---

## ⚠️ 항상 지킬 것 (매 커밋 전 확인)

새 요구사항은 옛 원칙과 정반대다 — 옛 카드1~5는 "실제 개인 기록은 PC에만, 공개엔 합성 데이터만"이 원칙이었지만, 새 카드1~5(Plan-Do-See)는 로그인이 없는 대신 **실제 내 계획·할일·실행기록**을 넣어야 완료 조건을 채운다. 두 원칙이 화면 안에서 섞이지 않도록 주의한다.

- [ ] 습관 기록기 쪽에는 옛 원칙대로 합성(가상) 데이터만 남아 있다 (실제 개인 기록·개인정보 없음)
- [ ] Plan-Do-See 쪽에 실제 내 계획/할일/기록을 넣을 때도 "남이 봐도 괜찮은 내용만" 원칙을 지킨다(민감한 일기·타인 정보 없음)
- [x] 비밀값(anon key 이외의 키·비밀번호 등)이 이 커밋에 없다 — `grep` 전수 점검 완료(2026-08-31)
- [x] `service_role` 키를 어디에도 넣지 않았다 — `grep` 전수 점검 완료(2026-08-31)
- 실수로 민감한 내용이 들어갔다면: 삭제 커밋만으로는 부족(기록에 남음) → 즉시 히스토리 재작성 후 force-push로 조치. 애초에 넣지 않는 게 최선.

---

## 과제 개요

계획(Plan) → 실제로 한 일(Do) → 돌아보기(See)가 하나로 이어지는 다이어리를 만든다. 로그인은 없고, 자료는 서버의 실제 데이터베이스(Supabase Postgres)에 저장한다. 링크를 아는 사람은 누구나 볼 수 있으므로 남이 봐도 괜찮은 내용만 넣는다. (원문: [ASSINGMENT.md](ASSINGMENT.md))

옛 습관 기록기(운동·독서 등 자유 항목 기록, 반복 체크리스트, 캘린더/테이블 뷰)는 새 요구사항에 없지만 별개 기능으로 이 앱에 계속 남긴다 — 화면 위 모드 전환으로 접근한다.

## 지향점

- 계획 ID는 그대로 두고 내용만 바뀐다 — 수정 이력은 DB 트리거가 자동 보존해 앱 코드 실수와 무관하게 남는다
- 완료 처리는 몇 번을 눌러도 기록·집계가 한 번만 늘어난다 — 버튼 비활성화가 아니라 DB 제약(RPC의 조건부 UPDATE)으로 보장한다
- 돌아보기의 모든 집계 숫자는 근거 기록으로 바로 갈 수 있다(드릴다운) — 숫자만 보여주고 끝나지 않는다
- 실행 기록은 계획의 예상값을 절대 덮어쓰지 않는다
- 로그인이 없다는 사실을 첫 화면에 항상 밝히고, 실제 내 자료를 넣되 공개해도 괜찮은 내용만 넣는다
- 옛 습관 기록기의 화면·기능·데이터(로컬 저장, 합성 데이터 원칙)는 건드리지 않고 그대로 유지한다

## 먼저 정할 것

- [x] 백엔드/DB를 정한다 → Supabase(Postgres), RLS로 로그인 없는 공개 읽기·쓰기를 명시적으로 통제
- [x] 옛 습관 기록기를 어떻게 할지 정한다 → 삭제하지 않고 그대로 유지, 새 기능과 한 페이지에서 모드 전환으로 공존
- [x] 전역 이름·화면 요소 id 충돌을 없앤다 → 새 코드는 `pds-app.js`/`pds-ui.js`로 분리해 IIFE로 감싸고, 유일하게 겹쳤던 `export-btn` id는 `pds-export-btn`으로 분리
- [x] 새 화면의 시각 테마를 정한다 → 별도 팔레트를 만들지 않고 옛 다크 테마 CSS 변수를 그대로 재사용
- [x] 날짜·시각 저장 규칙을 정한다 → 날짜 전용 컬럼은 Asia/Seoul 달력 `date`, 시각 컬럼은 UTC `timestamptz`

## 최종 제출물

검증 안내서·AI 3줄은 이 문서 안의 해당 섹션에 바로 작성한다. 제출 화면에는 그 두 섹션만 옮겨 붙인다 — 나머지(진행 상태 표, 카드별 상세, 완주 체크리스트, 마무리 체크리스트, 설계 이력, 부록 등)는 작업 기록용으로 이 문서에만 남는다.

### 완주 체크리스트

[ASSINGMENT.md](ASSINGMENT.md) "제출물" 섹션에 있는 5개 항목 원문 그대로. 카드별 세부 통과 기준(`T06-C##`)과는 별개로, 제출 전 마지막에 이 5개만 다시 확인한다.

- [x] 계획 → 실제로 한 일 → 돌아보기가 서버 데이터베이스로 이어집니다. — Supabase Postgres 스키마로 구현, 자동 테스트로 전체 흐름(계획 생성→할일→완료/실행기록→돌아보기 집계) 확인
- [x] 내가 실제로 세운 계획과 할 일과 실행 기록이 들어 있습니다. — "T06 플랜두씨 다이어리 앱 만들기" 계획(할 일 6개, 실행 기록 5개, 돌아보기 메모 1개) 실입력 완료(진행 상태 #18~20 참고)
- [x] 집계 숫자를 눌러 그 숫자가 나온 기록으로 갈 수 있습니다. — 돌아보기 드릴다운 자동 테스트로 확인
- [x] 아직 로그인이 없다는 안내가 첫 화면에 적혀 있습니다. — 헤더에 고정 문구 표시
- [ ] 최종 소스, 스크립트 삽입, 비밀값 노출, 외부 공개 여부를 확인했습니다. — 스크립트 삽입(XSS)은 자동 테스트로 확인 완료. 비밀값 노출 점검·외부 공개(배포·시크릿창) 확인은 아직(진행 상태 #22~24 참고)

---

## 과제 카드별 진행 상황

카드별 원문(하는 일/완료 목표/첫 행동/통과 기준/막히는 지점/남길 것)은 [ASSINGMENT.md](ASSINGMENT.md) 참고. 여기서는 진행 상황과 남긴 결과물만 기록한다.

### 카드 1 — 계획 세우기 ✅
`plans`(현재 값) + `plan_revisions`(BEFORE UPDATE 트리거가 자동 적재하는 수정 전 스냅샷) 구현. 자동 테스트로 계획 생성 확인, 연속 2회 수정 시 이력 2건이 오래된 순으로 정확히 쌓이는 것 확인. 남긴 것: `supabase/schema.sql`의 `plans`/`plan_revisions` 테이블·트리거, 자동 테스트 로그.

### 카드 2 — 할 일 다루기 ✅
`todos` 테이블 + CRUD, 검색(제목)·필터(상태/태그)·정렬(마감일/우선순위/등록순, 동률 규칙 화면에 문구로 명시) 구현. 자동 테스트로 생성·수정(제목·마감일·우선순위·태그·예상시간 전부 변경 확인)·완료·되돌리기·삭제·검색·정렬·상태필터·태그필터 전부 값 단위로 확인 완료.

### 카드 3 — 실제로 한 일 적기 ✅
`execution_logs` 테이블 + `complete_todo` RPC(멱등 완료 처리). 자동 테스트로 완료 연타 시 실행기록 1건만 증가하는 것을 직접 확인(연속 호출 2회 → `execution_logs` 1건). 실행 기록 저장이 `plans`/`todos`의 예상값을 건드리지 않는 것도 실제 값 대조로 확인(완료 처리 전·후 계획 필드 전부 동일, 할 일의 `estimated_minutes`도 그대로 유지).

### 카드 4 — 돌아보기, 그리고 다음 계획으로 ✅
집계(계획수·완료수·지연수·막힘수)와 드릴다운을 자동 테스트로 확인. 예상/실제/차이 시간 계산값도 정확한 숫자로 검증 완료 — 할 일 3건(예상 30+20+10=60분), 2건 완료(실제 45+5=50분, 1건은 미완료)로 구성한 뒤 돌아보기 표가 정확히 `계획수 3 · 완료 2 · 예상 60 · 실제 50 · 차이 -10`으로 뜨는 것을 확인. "다음 계획으로 넘길 한 줄"도 실사용 흐름으로 검증 — 돌아보기에서 메모 저장 → 새 계획 만들기 폼의 "이어받을 돌아보기 메모" 드롭다운에 그 메모가 뜸 → 선택해서 새 계획 생성 → 그 계획의 `carried_note_id`가 실제로 그 메모 id와 연결됨(단순 복사가 아니라 DB 참조로 이어짐)을 확인.

### 카드 5 — 내 것으로 채우고, 잃지 않게 ⬜
실제 데이터 입력 완료 — "T06 플랜두씨 다이어리 앱 만들기"(이 앱을 만드는 실제 작업)를 계획으로 등록, 할 일 6개(5개 완료)·실행 기록 5개·돌아보기 메모 1개를 anon key REST API로 직접 입력(2026-08-31). 시각은 git 커밋 이력의 실제 작업 시각을 근거로 삼았고, 병합 작업(할 일4)의 막힌 이유는 실제로 있었던 파일 실수 삭제→복구 사건을 그대로 기록. 첫 화면 안내 문구(로그인 없음)는 구현·표시됨. 내보내기(JSON)는 자동 테스트로 동작 확인. 스크립트 모양 글자가 리터럴로만 보이는 것도 자동 테스트로 확인(`<script>` 문자열을 할 일 제목에 넣고 실행되지 않음·화면엔 글자 그대로 표시됨을 검증). 남은 것: 배포 화면에서 실데이터 재확인(WebFetch는 정적 HTML만 봐서 클라이언트 JS 렌더링 결과 확인 불가, 브라우저로 직접 확인 필요)·시크릿창 확인·`contracts/pds-schema-v2.json` 최신화 확인. 참고: 운영 DB에 예전 캘린더 레인 기능 테스트용으로 남아 있던 `test`/`test 1`/`test 2`/`test 3` 계획 4개는 `plans` 테이블에 anon delete 권한이 없어(설계상 계획 삭제 기능 자체를 안 둠) 코드로 못 지웠다 — 남겨 둬도 통과 기준(계획 1개 이상 실데이터)엔 지장 없으나, 원하면 Supabase 대시보드 SQL 편집기에서 직접 지울 수 있다.

---

## 마무리 체크리스트 (카드별 통과 기준 하나하나 대조)

위 "완주 체크리스트"(5개, 제출용 요약)와는 별개로, 카드 1~5의 `T06-C##` 세부 통과 기준을 하나씩 대조한 작업용 상세 목록.

### 카드 1 — 계획 세우기
- [x] T06-C04 계획에 기간이 저장된다 — 자동 테스트로 확인
- [x] T06-C05 계획에 우선순위가 저장된다 — 자동 테스트로 확인
- [x] T06-C06 계획에 성공 기준이 저장된다 — 자동 테스트로 확인
- [x] T06-C07 계획에 예상 시간이 저장된다 — 자동 테스트로 확인
- [x] T06-C08 계획을 고쳐도 고치기 전 계획이 그대로 남아 있다 — 연속 2회 수정 → 이력 2건 정확히 저장(`security definer` 패치 후 재검증)

### 카드 2 — 할 일 다루기
- [x] T06-C09 할 일을 만들 수 있다 — 자동 테스트로 확인
- [x] T06-C10 할 일의 내용을 고칠 수 있다 — 자동 테스트로 확인(제목·마감일·우선순위·태그·예상시간 5개 필드 전부 변경 후 값 일치 확인)
- [x] T06-C11 할 일을 완료로 바꿀 수 있다 — 자동 테스트로 확인
- [x] T06-C12 완료한 할 일을 다시 진행 중으로 되돌릴 수 있다 — 자동 테스트로 확인
- [x] T06-C13 할 일을 지울 수 있다 — 자동 테스트로 확인(소프트 삭제, 목록에서 제외됨)
- [x] T06-C14 할 일에 마감일을 저장할 수 있다 — 자동 테스트로 확인
- [x] T06-C15 할 일에 우선순위를 저장할 수 있다 — 자동 테스트로 확인
- [x] T06-C16 할 일에 태그를 저장할 수 있다 — 자동 테스트로 확인
- [x] T06-C17 할 일에 예상 시간을 저장할 수 있다 — 자동 테스트로 확인
- [x] T06-C18 할 일을 검색할 수 있다 — 자동 테스트로 확인(제목 검색 → 1건으로 필터링)
- [x] T06-C19 할 일을 조건으로 걸러 볼 수 있다 — 자동 테스트로 확인(태그 필터·상태 필터 각각 대상 1건만 남기고 걸러짐)
- [x] T06-C20 화면에 밝혀 둔 기준대로 할 일이 정렬된다 — 자동 테스트로 확인(마감일순 정렬 결과 순서 일치), 화면에 동률 규칙 문구 표시

### 카드 3 — 실제로 한 일 적기
- [x] T06-C23 실행 기록에 시작 시각이 저장된다 — 자동 테스트로 확인
- [x] T06-C24 실행 기록에 끝난 시각이 저장된다 — 자동 테스트로 확인
- [x] T06-C25 실행 기록에 실제로 걸린 시간이 저장된다 — 자동 테스트로 확인
- [x] T06-C26 실행 기록에 막혔던 이유가 저장된다 — 자동 테스트로 확인(막힘 사유 입력 → 돌아보기 막힘 수에 반영됨)
- [x] T06-C27 실행 기록을 저장해도 원래 계획 값은 덮어쓰이지 않는다 — 자동 테스트로 값 대조 확인(완료 처리 전·후 계획의 모든 필드 동일, 할 일의 예상 시간도 그대로)
- [x] T06-C21 완료 버튼을 연달아 두 번 눌러도 완료 기록은 한 건만 남는다 — 자동 테스트로 확인(RPC 연속 호출 2회 → 실행기록 1건)
- [x] T06-C22 그때 돌아보기의 완료 수도 정확히 1만 늘어난다 — 집계가 `todos.status` 원본에서 매번 계산되므로 C21과 같은 메커니즘으로 보장

### 카드 4 — 돌아보기, 그리고 다음 계획으로
- [x] T06-C28 돌아보기의 계획 수가 그 계획에 딸린, 지우지 않은 할 일 수와 같다 — 자동 테스트로 확인(할 일 3건 → 계획 수 3)
- [x] T06-C29 돌아보기의 완료 수가 그중 지금 완료 상태인 할 일 수와 같다 — 자동 테스트로 확인(2건 완료 → 완료 수 2)
- [x] T06-C30 돌아보기의 지연 수가 완료되지 않았고 마감일이 지난 할 일 수와 같고, 완료한 할 일을 지연으로 두 번 세지 않는다 — 자동 테스트로 확인(완료 처리한 항목이 지연 수에서 빠짐)
- [x] T06-C31 돌아보기의 막힘 수가 막힌 이유가 하나라도 적힌 할 일 수와 같다 — 자동 테스트로 확인
- [x] T06-C32 돌아보기의 예상/실제/차이 시간 계산이 정확하다 — 자동 테스트로 값 직접 확인(예상 30+20+10=60, 실제 45+5=50, 차이 -10 — 화면 표시값과 정확히 일치)
- [x] T06-C83 돌아보기의 집계 숫자를 눌러 그 숫자가 나온 기록으로 갈 수 있다 — 자동 테스트로 확인(막힘 수 클릭 → 드릴다운 1건 표시)
- [x] T06-C33 돌아보기에서 정한 고칠 점 한 건이 다음 계획으로 넘어간다 — 실사용 흐름으로 확인: 돌아보기에서 메모 저장 → 계획 만들기 폼 드롭다운에 노출 → 선택해 새 계획 생성 → 새 계획의 `carried_note_id`가 그 메모 id로 실제 연결됨(DB 참조, 단순 복사 아님)

### 카드 5 — 내 것으로 채우고, 잃지 않게
- [x] T06-C34 계획·할 일·실행 기록·돌아보기 자료가 서버의 실제 데이터베이스에 저장된다 — Supabase Postgres, REST로 확인
- [ ] T06-C35 새로고침 뒤 저장된 값이 그대로 복원된다 — 데이터가 서버에만 있고 클라이언트 캐시가 없어 구조상 보장되나, 새로고침 자체를 자동 테스트로 재현하지 않음
- [x] T06-C78 내가 실제로 세운 계획이 하나 이상 들어 있다 — "T06 플랜두씨 다이어리 앱 만들기" 1건(2026-08-31)
- [x] T06-C79 그 계획에 딸린 할 일이 다섯 개 이상 들어 있다 — 6개(2026-08-31)
- [x] T06-C80 실제로 한 일 기록이 세 개 이상 들어 있다 — 5개(2026-08-31)
- [x] T06-C81 돌아보기 화면이 그 자료로 채워져 있고, 집계 숫자가 모두 0이지는 않다 — REST 직접 조회로 확인(계획 6·완료 5·지연 0·막힘 1·예상 570·실제 324·차이 -246)(2026-08-31)
- [x] T06-C36 내 자료 전체를 파일 하나로 내보낼 수 있다 — 자동 테스트로 확인(오류 없이 실행)
- [x] T06-C82 첫 화면에 로그인 없음 안내 문구가 적혀 있다 — `index.html` 헤더에 고정 표시
- [x] T06-C58 브라우저 코드·배포 파일·네트워크 응답·콘솔·Git 기록 어디에도 비밀키 원문이 없다 — `grep`으로 저장소 전체 점검 완료. JWT 형태 문자열은 `config.js`의 anon key 하나뿐이고 payload 디코드 결과 `"role":"anon"`(공개 키) 확인. `service_role`/`secret_key`/DB 비밀번호/`.env` 파일 등은 어디에도 없음
- [x] T06-C57 스크립트 모양 글자를 저장해 화면에 띄워도 실행되지 않고 글자 그대로 보인다 — 자동 테스트로 확인
- [x] T06-C59 짧은 확인 방법 4단락이 각각 나뉘어 적혀 있다 — "검증 안내서" 섹션에 작성(2026-08-31)
- [x] T06-C60 제출문에 AI 3줄이 각각 나뉘어 적혀 있다 — "AI 3줄" 섹션에 초안 작성, 본인 검토 필요(2026-08-31)
- [ ] T06-C01 제출한 모든 URL이 시크릿 창에서 로그인 절차 없이 열린다 — 배포 후 확인 예정

---

## 검증 안내서

① 어디로 가나요 — https://whiteclover0542.github.io/plan_record/ 접속(기본 화면이 "계획 다이어리 (Plan·Do·See)" 모드).
② 세 단계 안에 무엇을 하나요 — (1) 계획 목록에서 "T06 플랜두씨 다이어리 앱 만들기" 계획을 연다 (2) 할 일 탭에서 그 계획에 딸린 할 일 목록(완료 5개·진행중 1개)을 본다 (3) 돌아보기 탭으로 이동해 집계 숫자(예: 막힘 수 1) 중 하나를 클릭한다.
③ 무엇이 보이면 통과인가요 — 계획·할 일·실행 기록이 실데이터로 채워져 있고, 돌아보기 집계(계획 6·완료 5·지연 0·막힘 1·예상 570분·실제 324분·차이 -246분)가 0이 아니며, 숫자를 클릭하면 근거가 된 기록으로 이동한다.
④ 안 될 때는 무엇이 보이나요 — 목록이 "기록이 없습니다" 같은 빈 상태로 보이거나, 개발자 콘솔에 Supabase REST 요청 실패(네트워크 오류·CORS·401 등)가 찍힌다.

## AI 3줄

① AI에게 맡긴 일 — DB 스키마 설계, Supabase 연동 코드와 화면 구현, 자동화 테스트, 문서(README/PROGRESS) 정리, 그리고 실제 계획·할 일·실행 기록을 anon key REST API로 DB에 직접 입력하는 작업(무엇을 넣을지는 내가 지정).
② 내가 직접 판단한 일 — 실데이터로 어떤 계획을 넣을지(이 앱을 만드는 실제 작업 자체로 하기로 결정), 옛 습관 기록기를 지우지 않고 새 기능과 병합해서 유지할지, 운영 DB에 남아있던 예전 테스트 데이터를 지우고 진행할지 같은 방향 결정.
③ AI 제안을 따르지 않은 일 — 병합 초기에 AI가 옛 습관 기록기 파일(app.js/ui.js/checklist.js 등)을 전부 지우고 새 앱으로 통째로 교체했는데, 테마·형식·기능은 유지하고 새 기능만 얹으면 된다고 판단해 git에서 전부 되돌리고 다시 병합하도록 지시함.

*(위 초안은 실제 진행 이력과 이 대화에서 오간 판단을 근거로 AI가 작성했다 — 제출 전 본인 목소리로 맞는지 검토·수정 필요)*

## 증거 보관 기준

- 제출 화면에는 "검증 안내서"·"AI 3줄" 두 섹션의 내용만 옮겨 붙인다.
- Plan-Do-See 쪽은 실제 내 자료를 화면에 그대로 노출하되(로그인 없는 과제 취지), 남이 봐도 괜찮은 내용만 넣는다.
- 습관 기록기 쪽은 옛 원칙대로 합성 데이터만 유지한다.
- 그 외 근거 자료(스키마 설계, 자동 테스트 로그, 트리거 버그 수정 과정 등)는 이 문서에만 남기고 제출 화면에는 올리지 않는다.

---

## 설계 결정 이력

> 필드·스키마·보안·병합 방식처럼 근거로 남겨야 할 결정만 기록. 옛 습관 기록기(카드1~5)의 설계 이력은 git 이력에 그대로 남아 있다.

- 2026-08-31: 요구사항 교체(습관 기록기 → Plan-Do-See). 백엔드는 Supabase(Postgres) 채택 — 무료 티어로 즉시 시작 가능, 프론트는 정적인 채로 GitHub Pages 유지 가능, RLS로 로그인 없는 공개 쓰기 모델을 명시적으로 통제할 수 있어서 선택.
- 2026-08-31: 계획 수정 이력은 앱 코드가 아니라 DB 트리거(BEFORE UPDATE)로 보존 — 클라이언트가 이력 저장을 빼먹거나 조작할 수 없게 구조적으로 보장.
- 2026-08-31: 완료 중복 방지는 버튼 비활성화가 아니라 `UPDATE ... WHERE status <> 'done'` 조건 + RPC로 DB 레벨에서 보장 — 네트워크 재시도·더블클릭 등 클라이언트 쪽 상황과 무관하게 안전.
- 2026-08-31: `plan_revisions` 트리거를 `security definer`로 바꿈 — 트리거가 호출자(anon) 권한으로 돌기 때문에, anon에게 그 테이블 INSERT 권한을 직접 주지 않고도(클라이언트가 이력을 임의로 못 쓰게) 트리거만 안전하게 적재하게 함.
- 2026-08-31: 할 일은 소프트 삭제(`deleted_at`), 계획은 삭제 기능 자체를 두지 않음 — 요구사항에 계획 삭제가 없고, 돌아보기 집계·실행기록이 계획을 참조하므로 실수로 지워 이력이 끊기는 걸 막기 위해 애초에 삭제 경로를 안 만듦.
- 2026-08-31: 날짜는 `date`(Asia/Seoul 달력), 시각은 `timestamptz`(UTC 저장) — 옛 습관 기록기 때 확인된 "존재하지 않는 날짜가 밀려서 통과되는 문제"를 피하려고 날짜·시각 컬럼 타입을 처음부터 분리.
- 2026-08-31: 한 차례 옛 습관 기록기 파일(app.js/ui.js/checklist.js/index.html/style.css/submission/card5-log.md/fixtures)을 전부 삭제하고 새 앱으로 통째로 교체했으나, "테마·형식·기능은 유지하고 기능만 추가하면 된다"는 피드백에 따라 git에서 전부 복구하고 병합하는 방향으로 되돌림.
- 2026-08-31: 병합 시 전역 이름 충돌을 막기 위해 새 코드를 `pds-app.js`/`pds-ui.js`로 분리하고 IIFE로 감쌈 — `pds-app.js`는 `window.PDS` 하나에만 필요한 것을 내보내고, `pds-ui.js`는 그걸 가져다 쓰되 내부 상태(`state`, `el` 등)는 전역에 남기지 않음. 유일하게 겹쳤던 HTML id(`export-btn`)는 새 쪽만 `pds-export-btn`으로 바꿈.
- 2026-08-31: 새 화면(Plan-Do-See)에 별도 색상 팔레트를 만들지 않고 옛 습관 기록기의 다크 테마 CSS 변수(`--bg`/`--panel`/`--accent-bg` 등)와 폰트를 그대로 재사용 — 한 페이지 안에서 두 기능이 다른 앱처럼 보이지 않게 하기 위함.

---

## 부록: 파일 구조

```
/
├── ASSINGMENT.md              # 동결된 요구사항 (Plan-Do-See)
├── PROGRESS.md                # 이 문서
├── README.md                  # (재작성 예정)
├── index.html                 # 습관 기록기 + Plan-Do-See 모드 전환 포함 단일 페이지
├── style.css                  # 옛 다크 테마 + Plan-Do-See 추가 스타일(같은 변수 재사용)
├── app.js / ui.js / checklist.js   # 옛 습관 기록기(로컬 저장, 변경 없음)
├── config.js                  # Supabase URL + anon key (공개 키)
├── pds-app.js                 # Plan-Do-See 데이터 계층(IIFE, window.PDS로 내보냄)
├── pds-ui.js                  # Plan-Do-See 렌더링 + 이벤트(IIFE) + 모드 전환 스위치
├── supabase/
│   └── schema.sql             # DDL 원본: 테이블, 트리거, RPC 함수, RLS 정책
├── contracts/
│   └── pds-schema-v2.json     # 스키마 계약 요약(표·컬럼·관계·시간대 규칙)
├── fixtures/v1-sample.json    # 옛 습관 기록기 v1 샘플(카드3 근거, 변경 없음)
├── card5-log.md                # 옛 습관 기록기 5일 실사용 로그(변경 없음)
└── submission/                 # 옛 습관 기록기 제출 증거(변경 없음, 새 카드용은 별도 작성 예정)
```

## 부록: 시간대·식별자 규칙

- 기준 시간대: `Asia/Seoul` (고정)
- 날짜 전용 컬럼(`period_start`/`period_end`/`due_date`): 시간 없는 `date`, Asia/Seoul 달력 기준
- 시각 컬럼(`created_at`/`started_at`/`ended_at`/`completed_at` 등): UTC로 저장(`timestamptz`), 화면은 Asia/Seoul로 변환 표시
- 지연 판정: `status <> 'done' AND due_date < 오늘(Asia/Seoul)`
- 고유 ID: Postgres `gen_random_uuid()`

## 부록: 열린 질문 / 리스크

- 카드5(실사용 데이터 입력)는 사용자가 직접 해야 하는 부분이라 AI가 대신 채울 수 없다 — 진행 상태 #18~21 참고
- 옛 습관 기록기와 Plan-Do-See가 한 페이지에 있으므로, 검증 안내서 작성 시 오너가 어느 모드를 봐야 하는지 첫 화면에서 바로 알 수 있게 문구를 신경 써야 한다(현재는 Plan-Do-See가 기본 화면)
