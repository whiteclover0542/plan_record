# T06 플랜두씨 다이어리 1 — 진행 관리

> 요구사항 원문은 [ASSINGMENT.md](ASSINGMENT.md)에 동결되어 있습니다. 이 문서는 진행 상황을 계속 갱신하는 관리 문서입니다.

## 진행 상태 (22개 작업)
✅ 완료 · 🔄 진행중 · ⬜ 미완료

| # | 작업 | 상태 | 비고 |
|---|------|------|------|
| 1 | 기록 항목 한 문장 정의 | ✅ | 무엇을 / 어떤 숫자·상태로 / 어느 날짜·시간대에 |
| 2 | 필드 정의표 작성 | ✅ | 고유 ID·날짜·기준 시간대·항목·값·단위·메모 |
| 3 | 기준 시간대·주 시작 요일 결정 | ✅ | Asia/Seoul · 월요일 시작 |
| 4 | 저장소 초기 구조 생성 | ✅ | index.html/style.css/app.js/ui.js, localStorage 기반 |
| 5 | 합성 기록 3건 준비 (카드1) | ✅ | "합성 기록 3건 채우기" 버튼으로 시연(운동·독서·게임 기록) |
| 6 | 기록 생성·조회·수정·삭제 구현 (카드1) | ✅ | app.js Records.create/update/remove, Playwright로 동작 검증 |
| 7 | 카드1 통과 기준 확인 | ✅ | 로컬 브라우저 테스트로 생성·수정·삭제·필수값 검증 확인 (스크린샷 확보) |
| 8 | 합성 기록 5건 저장 후 새로고침 전·후 대조 (카드2) | ✅ | Playwright로 새로고침 전·후 5건 목록 일치 확인 |
| 9 | JSON 또는 CSV 내보내기 구현 | ✅ | JSON 선택, `ImportExport.exportJSON` + 다운로드 버튼 |
| 10 | 가져오기·복원 구현 | ✅ | 가져오기 시 전체 교체(복원), 내보낸 파일로 원복 확인 |
| 11 | 전체 삭제 구현 | ✅ | 확인창 후 `Records.removeAll()`, 0건 확인 |
| 12 | 손상 파일 거부·오류 표시 확인 (카드2) | ✅ | 잘못된 JSON·필수값 누락 파일 모두 거부, 기존 5건 유지, 오류 문구 표시 확인 |
| 13 | v1 원본 보관 + 새 필드·`schemaVersion` 추가 (카드3) | ✅ | `fixtures/v1-sample.json`(합성 3건, schemaVersion 없음/1) 보관, `tags` 필드 추가 |
| 14 | v1 → v2 자동 변환 구현·멱등성 확인 (카드3) | ✅ | `migrateRecordToV2`/`migrateAllToV2`(app.js), Playwright로 변환·재로드 2회 후 값 불변 확인 |
| 15 | 경계·누락·중복·잘못된 값 합성 자료 준비 (카드4) | ✅ | 아래 예상 결과표 + Playwright 테스트(월/일 경계, 누락, 중복 id, 잘못된 날짜, 비숫자 값 6종) |
| 16 | 처리 규칙 적용 + 주간 요약 구현 (카드4) | ✅ | `WeeklySummary.compute`(app.js) + 화면 "주간 요약" 표, 제외 사유 표시 |
| 17 | 서로 다른 실제 날짜 5일 기록 확보 (카드5) | ⬜ | 대기 기간, 능동 작업시간에서 제외 |
| 18 | 반복된 불편 1건 개선 구현 (카드5) | ⬜ | |
| 19 | 잘못 입력한 기록 수정 + 요약값 동기화 확인 (카드5) | ⬜ | |
| 20 | 공개 배포 및 공개 주소 확정 | ✅ | https://whiteclover0542.github.io/plan_record/ (GitHub Pages, 200 확인, 카드1~4 반영) |
| 21 | 검증 안내서 작성 (이 문서 "검증 안내서" 섹션) | ⬜ | |
| 22 | AI 3줄 작성 (이 문서 "AI 3줄" 섹션) | ⬜ | |

---

## ⚠️ 항상 지킬 것 (매 커밋 전 확인)
공개 화면·파일·제출 기록에 실제 개인 기록·개인정보·비밀값이 남으면, 나중에 파일에서 지워도 한 번 커밋된 내용은 기록에 남아 계속 공개될 수 있다.

- [ ] 실제 개인 기록·이름·연락처 등 비공개 정보가 이 커밋에 없다
- [ ] 공개 화면·파일에는 합성 데이터 또는 가림 처리한 집계값만 있다
- [ ] 비밀값(키·토큰 등)이 이 커밋에 없다
- 실수로 들어갔다면: 삭제 커밋만으로는 부족(기록에 남음) → 즉시 히스토리 재작성 후 force-push로 조치. 애초에 넣지 않는 게 최선.

---

## 과제 개요
습관, 운동, 게임 기록, 학습 시간 등 실제로 남기고 싶은 항목을 기록·수정·삭제하고 다시 불러오는 개인 기록기를 만든다. 계획시간 5~6시간에 더해 별도 경과 기간 5일(작업시간 합계 제외)이 필요하다. (원문: [ASSINGMENT.md](ASSINGMENT.md))

## 지향점
- 고유 ID 기준으로 정확한 한 건만 수정·삭제한다 — 목록 갱신 시 항상 ID로 대상을 특정한다
- 필수값이 빈 기록은 애초에 저장하지 않는다
- 형식이 바뀌어도 기존 값은 보존한다 — 변환은 멱등적으로, 반복 실행해도 늘거나 다시 바뀌지 않게 만든다
- 유효하지 않은 값(잘못된 날짜·누락·중복 등)은 정한 규칙대로 저장·거부·별도 표시하고 집계에는 절대 섞지 않는다
- 실제 개인 기록은 PC에만 두고, 공개·제출에는 합성 데이터 또는 가림 처리한 집계값만 사용한다

## 먼저 정할 것

- [x] 기록 항목을 한 문장으로 정의한다: 무엇을 / 어떤 숫자나 상태로 / 어느 날짜와 시간대에 (정의는 아래 "부록: 필드 정의" 참고)
- [x] 필드를 정한다: 고유 ID, 날짜, 기준 시간대, 항목, 값, 단위, 메모 → 아래 "부록: 필드 정의" 표 확정
- [x] 기준 시간대와 주 시작 요일을 정한다. → 기준 시간대 `Asia/Seoul`(고정), 주 시작 요일 `월요일`
- [x] 합성 데이터만 공개·제출에 사용한다. → "합성 기록 3건 채우기" 버튼으로만 예시 데이터 생성, 실제 개인 기록은 코드/저장소에 없음
- [x] 실제 개인 기록과 개인정보·비밀값을 공개 화면·파일·제출 기록에서 제외한다. → 백엔드 없음(순수 프론트엔드), 서버·비밀값 자체가 존재하지 않음

## 최종 제출물 (2개)
별도 파일 대신 이 문서 안의 "검증 안내서"·"AI 3줄" 섹션에 바로 작성한다. 제출 화면에는 그 두 섹션의 내용만 옮겨 붙인다 — 나머지(진행 상태 표, 카드별 작업, 설계 이력, 부록 등)는 작업 기록용으로 이 문서에만 남는다.

---

## 과제 안내 (5카드)

카드별 원 요구사항(하는 일/첫 행동/통과 기준/막힐 때)은 [ASSINGMENT.md](ASSINGMENT.md) 참고. 완료된 카드는 남긴 결과물만 기록.

### 카드 1 — 기록의 구조 ✅
CRUD 구현 완료. 남긴 것: 필드 정의표(부록), 합성 기록 3건, CRUD(`app.js` Records.*)

### 카드 2 — 내보내기와 전체 삭제 ✅
내보내기·가져오기(전체 교체)·전체 삭제 구현 완료. 남긴 것: 내보낸 파일 샘플, 손상 파일 테스트 기록

### 카드 3 — 기존 기록을 잃지 않는 변경 ✅
v1→v2 자동 변환(멱등) 완료. 남긴 것: v1 원본 파일(`fixtures/v1-sample.json`), 변환 전·후 대조 기록

### 카드 4 — 날짜와 잘못된 값 ✅

**예상 결과표** (구현 전 작성)

| # | 입력 | 처리 | 이유 |
|---|---|---|---|
| 1 | 날짜 = 해당 주 월요일 (예: `2026-08-24`) | 저장 + 집계 포함 | 정상 값. 해당 주의 첫 날 — 이전 주로 새면 안 됨 |
| 2 | 날짜 = 해당 주 일요일 (예: `2026-08-30`) | 저장 + 집계 포함 | 정상 값. 해당 주의 마지막 날 — 다음 주로 새면 안 됨 |
| 3 | 필수값 누락 (예: `item`이 빈 문자열) | UI/가져오기에서는 거부(저장 안 함). 저장소에 이미 존재하는 경우 집계에서 별도 표시 후 제외 | 필수값 없이는 유효한 기록이 아님 |
| 4 | id 중복 (동일 id 두 번 등장) | 가져오기는 거부. 저장소 레벨에서 발견되면 먼저 나온 것만 채택하고 나머지는 집계에서 별도 표시 후 제외 | 고유 ID 원칙상 한 id는 기록 한 건만 대표해야 함 |
| 5 | 날짜 형식 오류 (예: `2026-13-40`, `2026-02-30`처럼 실존하지 않는 날짜) | UI/가져오기에서는 거부. 저장소에 존재하면 집계에서 별도 표시 후 제외(어느 주에도 포함하지 않음) | 존재하지 않는 날짜는 주 계산 자체가 불가능 |
| 6 | 값이 숫자가 아님 (예: `"abc"`) | UI/가져오기에서는 거부. 저장소에 존재하면 집계에서 별도 표시 후 제외(합계 계산에서 제외) | 숫자가 아닌 값은 합계에 더할 수 없음 |

**합성 경계·오류 자료**: 위 6가지 케이스를 모두 포함한 합성 기록 세트를 Playwright 테스트에서 저장소에 직접 주입해 검증한다(UI 정상 경로로는 3·5·6번이 애초에 거부되어 저장되지 않으므로, 저장소가 오염된 상황을 가정한 방어 로직 검증).

**실제 결과 대조** (2026-08-27, Playwright)
- 월요일(`2026-08-24`)·일요일(`2026-08-30`) 기록 2건 → 주간 요약에 "2026-08-24 ~ 2026-08-30, 2건, 운동: 30"으로 정상 집계 (기대와 일치)
- 필수값 누락 1건, 중복 id 1건, 잘못된 날짜(`2026-02-30`) 1건, 비숫자 값(`"abc"`) 1건 → 총 4건이 집계에서 제외되고 사유가 화면에 표시됨, 나머지 집계값은 오염되지 않음 (기대와 일치)
- UI 폼에서 `<input type="date">`에 존재하지 않는 날짜를 직접 대입해도 브라우저가 값 자체를 받지 않아 빈 값 오류로 거부됨 — 정상 입력 경로에서는 브라우저가 1차 방어, 가져오기·저장소 오염 시에는 `isValidDateString`이 2차 방어

### 카드 5 — 5일 사용 후 한 가지 개선 ⬜
- 하는 일: 서로 다른 실제 날짜 5일에 사용하고 반복해서 불편했던 한 가지를 고치기
- 첫 행동: 카드1을 마친 날부터 매일 실제 기록을 PC에 1건 이상 남기기
- 통과 기준: 가상 기록을 고치면 해당 행과 관련 요약값이 같은 화면에서 함께 바뀐다
- 막힐 때: 5일 대기 중에는 카드2~4 작업을 먼저 채워 대기 시간을 낭비하지 않는다
- 남길 것: 5일치 실제 기록 로그(PC 전용, 비공개), 잘못 입력 수정 전·후 화면

---

## 마무리 체크리스트 (최종 검증 기준)

1. ⬜ **공개 주소에서 합성 기록을 추가·수정·삭제할 수 있다**
   - 판단: 공개 화면에서 세 동작 모두 성공
   - 증거: 배포된 화면 스크린샷
2. ⬜ **고유 ID로 정확한 한 건만 수정·삭제된다**
   - 판단: 동일 항목 여러 건 중 대상 1건만 바뀌고 나머지는 그대로임을 확인
   - 증거: 수정·삭제 전·후 목록 대조
3. ⬜ **필수값이 빈 기록은 저장되지 않는다**
   - 판단: 빈 값으로 저장 시도 시 거부됨
   - 증거: 빈 값 입력 테스트 기록
4. ⬜ **새로고침 뒤에도 기록 수와 값이 유지된다**
   - 판단: 새로고침 전·후 ID·날짜·값이 동일
   - 증거: 대조 기록
5. ⬜ **내보낸 파일에서 ID·날짜·값·단위가 복원된다**
   - 판단: 내보내기 → 가져오기 후 원본과 일치
   - 증거: 내보낸 파일 샘플, 복원 결과 대조
6. ⬜ **전체 삭제 뒤 기록은 0건이다**
   - 판단: 전체 삭제 실행 후 목록 확인
   - 증거: 삭제 전·후 화면
7. ⬜ **손상 파일은 기존 기록을 바꾸지 않고 오류 이유를 표시한다**
   - 판단: 손상 파일 가져오기 시도 시 기존 기록 유지 + 오류 문구 표시
   - 증거: 손상 파일 테스트 기록
8. ⬜ **v1·v2 변환 뒤 건수와 기존 값이 유지된다**
   - 판단: 변환 전·후 ID·날짜·값·단위 대조 일치
   - 증거: 변환 전·후 대조표
9. ⬜ **변환을 두 번 실행해도 기록이 늘거나 다시 바뀌지 않는다(멱등성)**
   - 판단: 동일 파일로 변환 함수 2회 실행 후 건수·값 비교
   - 증거: 반복 실행 결과 기록
10. ⬜ **주간 요약의 기간과 집계값이 보인다**
    - 판단: 화면에 기준 시간대·주 시작 요일에 맞는 기간과 합계가 표시
    - 증거: 요약 화면 캡처
11. ⬜ **잘못된 날짜·값이 집계에서 제외된다**
    - 판단: 예상 결과표와 실제 집계 결과 일치
    - 증거: 카드4 예상 결과표 대조
12. ⬜ **실제 개인 기록·개인정보·비밀값이 공개 화면·파일·제출 기록에 0건이다**
    - 판단: 8절 "항상 지킬 것" 체크리스트 통과
    - 증거: 최종 전체 검색 결과
13. ⬜ **검증 안내서가 3단계 이내, 30초 안에 끝난다**
    - 판단: 어디로/무엇을/통과기준/안될때 4항목 모두 작성, 행동이 3단계 이하
    - 증거: 이 문서의 "검증 안내서" 섹션
14. ⬜ **오너가 공개 결과를 15초 안에 판정할 수 있다**
    - 판단: 첫 화면만으로 통과 여부가 바로 드러남
    - 증거: 배포 화면 첫 스크린샷
15. ⬜ **AI 3줄이 작성되었다**
    - 판단: 맡긴 일·판단한 일·안 들은 일 각 1줄
    - 증거: 이 문서의 "AI 3줄" 섹션

---

## 검증 안내서
> ⬜ 카드1~5 완료 후 작성

**어디로 가나요**
`미정`

**무엇을 하나요** (3단계)
1. `미정`
2. `미정`
3. `미정`

**무엇이 보이면 통과인가요**
- 기록 행과 요약값이 함께 바뀐다
- 내보내기·가져오기·전체 삭제와 오류 이유가 보인다
- 주간 요약 기간과 집계값이 보인다

**안 될 때**
- 오류 문구: `미정`
- 확인할 항목: `미정`
- 복구 방법: `미정`

## AI 3줄

- **AI에게 맡긴 일**: CRUD·내보내기/가져오기·v1→v2 변환·주간 요약 등 `app.js` 구현 전체를 AI에게 맡겼다.
- **내가 판단한 일**: 필드 정의, 기준 시간대(`Asia/Seoul`)·주 시작 요일(월요일), 공개용 데이터는 합성 데이터만 쓴다는 원칙은 직접 정했다.
- **AI 말을 안 들은 일**: 날짜 검증에서 AI가 제안한 `Date.parse` 방식을 쓰지 않았다 — `2026-02-30`처럼 존재하지 않는 날짜가 3월로 밀려 통과되는 문제가 있어, 왕복 검증하는 `isValidDateString`을 직접 만들어 대신 썼다.

---

## 증거 보관 기준
- 제출 화면에는 위 "검증 안내서"·"AI 3줄" 두 섹션의 내용만 옮겨 붙인다.
- 검증 안내서는 공개 주소를 바로 열어 3단계 이내 행동으로 30초 안에 끝나게 쓴다.
- 오너는 공개 주소의 현재 결과를 15초 안에 판정한다.
- 그 외 근거 자료(필드 정의표, 변환 전·후 대조, 손상 파일 테스트, 예상 결과표 등)는 이 문서의 "과제 안내" 섹션에만 남기고 제출 화면에는 올리지 않는다.

---

## 설계 결정 이력
> 필드·스키마·보안처럼 근거로 남겨야 할 결정만 기록.

- 2026-08-27: 기록 구조 확정 — item(자유 텍스트, 카테고리 고정 없음) + value + unit + date
- 2026-08-27: 기준 시간대 `Asia/Seoul` 고정, 주 시작 요일 `월요일`(카드4의 "월 00:00~일 23:59" 요구사항에 맞춤)
- 2026-08-27: 순수 HTML/CSS/JS + localStorage 선택 — 빌드 없이 GitHub Pages 배포 가능, 백엔드 없어 비밀값 노출 표면 최소화
- 2026-08-27: 내보내기 JSON, 가져오기는 "전체 교체(복원)" 방식 — ID 충돌 회피. 형식 오류 시 기존 데이터 보존 + 오류만 표시(`ImportExport.parseImport`)
- 2026-08-27: 카드3에서 `tags`(선택) 추가 + `schemaVersion` 1→2. `migrateRecordToV2`는 이미 v2면 원본 그대로 반환해 멱등성 보장. v1 원본은 `fixtures/v1-sample.json`
- 2026-08-27: 날짜 검증은 `Date.parse` 대신 `isValidDateString`(왕복 검증) 사용 — `Date.parse('2026-02-30')`가 3월로 밀려 통과하는 문제 회피. `WeeklySummary.compute`는 저장소 원본을 신뢰하지 않고 중복 id·필수값 누락·잘못된 날짜·비숫자 값을 걸러 사유와 함께 제외

---

## 부록: 필드 정의 (카드1 확정)

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| id | string (UUID) | O | 기록 한 건을 특정하는 고유 값. 수정·삭제는 항상 이 값으로 대상을 찾는다 |
| date | string `YYYY-MM-DD` | O | 기준 시간대(`Asia/Seoul`) 기준 날짜 |
| timezone | string | O | 고정값 `"Asia/Seoul"` |
| item | string | O | 기록 항목명 (예: 운동, 독서, 게임, 학습) — 자유 텍스트, 카테고리 고정 없음 |
| value | number | O | 값 (예: 30, 1, 5000) |
| unit | string | O | 값의 단위 (예: 분, 회, kg, 페이지) |
| memo | string | X (선택) | 메모 |

카드3에서 확정한 v2 형식(현재 사용 중): 위 v1 필드 + 아래 2개 추가.

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| tags | string[] | X (선택, 기본 `[]`) | 태그 목록 (예: `["실내", "아침"]`) |
| schemaVersion | number | O | `1`(구버전) 또는 `2`(현재). 저장·가져오기 시 1은 자동으로 2로 변환 |

## 부록: 파일 구조

```
/
├── ASSINGMENT.md   # 동결된 요구사항 (수정 금지)
├── PROGRESS.md     # 이 문서
├── index.html      # 화면 구조 (기록 폼·목록·요약·내보내기 영역)
├── style.css       # 스타일
└── app.js          # storage(localStorage) + CRUD + 요약 로직
```

## 부록: 시간대·식별자 규칙
- 기준 시간대: `Asia/Seoul` (고정, 모든 기록에 동일 적용)
- 주 시작 요일: `월요일` (월요일 00:00 ~ 일요일 23:59, 기준 시간대 기준)
- 날짜 식별값: 기준 시간대의 `YYYY-MM-DD` 문자열
- 고유 ID: `crypto.randomUUID()` 사용 (브라우저 내장, 외부 의존성 없음)

## 부록: 열린 질문 / 리스크
- 카드5는 서로 다른 실제 날짜 5일이 필요 — 대기는 작업시간에서 제외하고 별도로 기록한다
- 필드·스키마 확정 전에는 카드1 구현을 시작하지 않는다(재작업 방지)
