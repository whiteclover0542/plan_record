# T07 플랜두씨 다이어리 2 — 진행 관리

> 요구사항 원문은 [ASSINGMENT.md](ASSINGMENT.md)에 동결되어 있다. 이 문서는 진행 상황을 계속 갱신하는 관리 문서다.
> 2026-09-01: T06(로그인 없는 Plan-Do-See 다이어리)에 이어 붙이는 과제. T06의 진행 상태·카드별 상세·설계 이력·검증 안내서·AI 3줄·최종 제출 문서는 전부 [archive/t06/](archive/t06/)에 그대로 보존돼 있다.
> 이 과제는 "실제 5일" 사용 로그를 요구한다(카드5) — Asia/Seoul 기준 서로 다른 실제 날짜 5일에 걸쳐 진행하며, 2일차 뒤·3일차 앞에 계획 규칙을 1회 바꾼다. 하루 만에 끝나는 과제가 아니다.

## 진행 상태

| # | 작업 | 상태 | 비고 |
|---|------|------|------|
| 1 | 문서 정리 — T06 문서를 `archive/t06/`으로 이동, 새 `ASSINGMENT.md`/`PROGRESS.md` 작성 | ✅ | |
| 2 | 인증 방식 결정: Supabase Auth (이메일+비밀번호) | ✅ | 이미 Supabase Postgres 위에서 동작 중이라 `auth.uid()`와 바로 연결됨. 검토했으나 고르지 않은 방법: 직접 구현(bcrypt+JWT, 세션 관리 부담 큼), 별도 인증 서비스(Auth0/Clerk, 인프라 중복) |
| 3 | DB 마이그레이션 설계 + `supabase/migration-t07-auth.sql`/`schema.sql`/`contracts/pds-schema-v2.json` 작성 | ✅ | 5개 표에 `user_id` 추가, RLS를 `authenticated`+`user_id=auth.uid()`로 재작성(anon 권한 전부 회수). UPDATE는 `using(true)+with check`로 명시적 403이 나도록 설계. 기존 T06 데이터는 지우지 않고 이관하는 4단계(nullable 추가→계정 생성→UPDATE→NOT NULL). `complete_todo`/`reopen_todo` 소유자 조건 추가, 계정 삭제용 `delete_my_account()` RPC 신설 |
| 4 | 로그인/가입/로그아웃 화면 + 세션 가드 구현 | ✅ | `index.html`(`#auth-gate` 신설, 첫 화면이 로그인 화면), `pds-auth.js`(신규, 로그인 게이트 로직), `pds-app.js`(`Auth` 래퍼 + `window.PDS_SB` 클라이언트 공유), `pds-ui.js`(로그인 전 자동 초기화 제거), `style.css`(로그인 화면 스타일). supabase-js CDN 버전 `2.112.4`로 고정 |
| 5 | 라이브 Supabase Auth REST API 1차 확인 (curl) | ✅ | anon key로 `POST /auth/v1/signup` 호출 확인 — 이 프로젝트는 **이메일 확인(Confirm email)이 켜져 있음**을 실측으로 확인(응답에 세션 없이 `confirmation_sent_at`만 있음) |
| 6 | `README.md` T07 진행 중 상태로 갱신 | ✅ | |
| 7 | SQL 마이그레이션 적용 완료 (사용자가 대시보드에서 실행) | ✅ | RLS 5개 표 전부 `relrowsecurity=true`, 정책 13개 전부 `roles={authenticated}`(anon 정책 0개), `still_null=0`(T06 계획 5건 전부 이관)까지 SQL Editor 결과로 직접 확인(2026-09-01) |
| 8 | curl 기반 카드1~4 근거 수집 (disposable 테스트 계정 2개, anon key만 사용) | ✅ | 아래 "curl 테스트 결과 요약" 참고. 로그인/가입/중복가입/문구 동일성/세션 발급·로그아웃·토큰 재사용/계정 간 교차 접근(읽기·수정·삭제·목록·비로그인 직접 요청)·계정 삭제까지 실측 완료. **중요 발견 2건**: ① 로그아웃(및 계정 삭제) 후에도 이미 발급된 access token은 자연 만료(1시간) 전까지 계속 통함(refresh token만 즉시 무효화됨) ② 남의 행 UPDATE/DELETE 시도가 설계한 대로 명시적 403이 아니라 조용한 0건 처리로 막힘(데이터는 안전하지만 응답 코드가 설계와 다름 — 원인 확인 대기) |
| 9 | 카드2 근거(비밀번호 저장 모습)·UPDATE 정책 실제 정의 확인 | ✅ | 실제 계정 해시 확인(`$2a$10$...`, bcrypt). 같은 비밀번호 두 계정(X/Y) 비교로 salt가 달라 저장 값이 서로 다름을 확인(T07-C104). `plans_update` 정책 실제 정의(`qual=true, with_check=user_id=auth.uid()`)로 #8의 "조용한 무효 처리" 원인 규명 완료(Postgres RLS가 UPDATE/DELETE 대상 행 탐색에도 SELECT 정책을 함께 적용하기 때문 — 설계 버그 아님). 테스트 계정 4개(A/B/X/Y) 전부 `delete_my_account()`로 정리 완료 |
| 10 | 5일 실사용 로그 (서로 다른 실제 날짜 5일, 2일차 뒤·3일차 앞 계획 규칙 변경 1회) | ⏳ | |
| 11 | 인증 구현 설명서 6항목 / 짧은 확인 방법 4줄 / AI와 내 판단 3줄 작성 | ⏳ | |
| 12 | 배포(GitHub Pages) 반영 — 커밋·push | ⏳ | 마이그레이션 적용 완료로 이제 push해도 안전 — 사용자 승인 후 진행 |
| 13 | 최종 SUBMISSION.md/HTML/PDF 작성 | ⏳ | |

## curl 테스트 결과 요약 (2026-09-01, disposable 테스트 계정 A/B로 진행)

anon key만 사용해 라이브 Supabase 프로젝트에 직접 요청을 보내 확인. 계정 A/B는 이 테스트 전용으로 새로 만든 계정이라 비밀번호를 어디에도 남길 필요가 없고(실제로도 안 남김), B는 테스트 뒤 `delete_my_account()`로 삭제 완료.

- **가입/중복가입**: 새 이메일 가입 → `200` + 세션 즉시 발급(이메일 확인 꺼짐 확인). 같은 이메일로 재가입 시도 → `422 user_already_exists`(T07-C98).
- **로그인 문구 동일성(T07-C99)**: 비밀번호 틀림과 존재하지 않는 이메일 둘 다 `400 {"error_code":"invalid_credentials","msg":"Invalid login credentials"}`로 **완전히 동일**.
- **세션(T07-C108~115)**: JWT access token(`alg: ES256`, 비대칭키 — 이 프로젝트는 서명용 공유 비밀키 자체가 없는 방식) + refresh token. `expires_in: 3600`(1시간). 로그인 상태로 내 자료 조회 → `200`. `POST /auth/v1/logout?scope=global` → `204`. 그 직후 **같은 주소·같은 방식**으로 같은 access token으로 재요청 → 여전히 `200`(⚠️ 아래 발견 참고). 반면 같은 refresh token으로 재발급 시도 → `400 refresh_token_not_found`(즉시 무효화 확인). 토큰은 URL이 아니라 `Authorization` 헤더로만 전달.
- **계정 간 자료 격리(T07-C116~126)**: A/B 각각 계획 1건씩 생성(`user_id`는 클라이언트가 안 보내도 DB 기본값 `auth.uid()`로 자동 채워짐, 이후 확인). A→B, B→A 양방향으로 읽기(`GET`)·수정(`PATCH`)·삭제(`DELETE`) 전부 시도 → 전부 `200` + 빈 배열(`[]`)로 실질적 무효 처리, 실제 값 대조로 상대방 자료가 그대로임을 확인(변경도 삭제도 안 됨). 목록 조회(`GET /rest/v1/plans`)는 본인 것 1건만 반환. **비로그인 직접 요청**(anon key만, Authorization 없음) → `401 permission denied for table plans`(anon 권한 자체가 없음 — 설계대로). **소유권 위조 테스트**: A가 B의 계획을 수정하며 본문에 `user_id`를 자기 자신으로 바꿔 "훔치기" 시도 → 역시 빈 배열로 무효. A가 **본인 소유** 계획의 `user_id`를 B로 바꾸려는 시도 → `403 {"code":"42501", message:"new row violates row-level security policy"}`로 명시 거절(with check가 실제로 동작함을 확인). A가 B의 계획 id를 `plan_id`로 지정해 할 일(todo)을 새로 끼워 넣으려는 시도 → `403 42501`로 명시 거절.
- **계정 삭제(T07-C134)**: B로 `delete_my_account()` 호출 → `204`. 그 뒤 같은 이메일/비밀번호로 재로그인 시도 → `400 invalid_credentials`(계정 자체가 사라짐). B의 옛 계획도 더는 조회 안 됨(FK `on delete cascade`로 실제 삭제).
- **비밀값 노출 점검**: 저장소 전체 `grep`으로 `service_role`/`secret_key`/JWT 패턴 재확인 — `config.js`의 anon key 하나뿐, 나머지는 전부 "안 쓴다"는 설명 텍스트일 뿐임을 확인. `.history/`(에디터 로컬 백업 폴더, git 미추적)에도 같은 문서 사본만 있고 비밀값 없음 — 실수로 커밋되지 않도록 `.gitignore`에 추가.

## 다음 단계

1. **배포 반영(push) 승인** — 마이그레이션이 라이브 DB에 적용됐으니 이제 push해도 배포 화면이 깨지지 않는다. 사용자 승인 후 커밋·push 진행 (git 원칙상 명시적 요청 없이 커밋하지 않음).
2. **화면(UI) 클릭으로 최종 재확인** — 지금까지는 curl로 Auth API를 직접 확인했다. 배포 후 실제로 로그인 화면 → 가입/로그인/로그아웃 버튼 클릭 → 내 T06 자료가 보이는지까지 화면으로 한 번 더 확인.
3. **5일 실사용 로그 시작(카드5)** — 1일차 질문·지표·단위·계산 규칙을 먼저 고정하고 Asia/Seoul 기준 서로 다른 실제 날짜 5일에 걸쳐 진행. 2일차 뒤·3일차 앞에 계획 규칙 1회 변경.
4. 인증 구현 설명서·검증 안내서·AI 3줄 최종 작성, SUBMISSION 문서 작성.

---

## ⚠️ 항상 지킬 것 (매 커밋 전 확인)

- [ ] 비밀번호·토큰·비밀키 원문이 이 저장소 어디에도 없다 (문서·코드·git 이력 전부) — 요청/응답 예시를 넣을 때 토큰은 `eyJhb…생략`처럼 앞부분만 남기고 자른다
- [ ] `service_role` 키를 어디에도 넣지 않는다 — 클라이언트에는 anon key만
- [ ] 제출 문서에 진짜 계정의 비밀번호를 적지 않는다 — 성공/거절 응답을 나란히 적고 비밀값은 가려서 적는다
- [ ] 습관 기록기(옛 카드1~5) 쪽은 그대로 유지 — 이번 과제 대상이 아님, 합성 데이터 원칙도 그대로
- [ ] 마이그레이션 적용 전에는 `origin/main`에 push하지 않는다 — 배포 화면이 깨지는 걸 막기 위함
- 실수로 민감한 내용이 들어갔다면: 삭제 커밋만으로는 부족(기록에 남음) → 즉시 히스토리 재작성 후 force-push로 조치. 애초에 넣지 않는 게 최선.

---

## 과제 개요

T06에서 만든, 로그인이 없어 링크를 아는 사람은 누구나 볼 수 있었던 Plan-Do-See 다이어리에 Supabase Auth로 가입·로그인·로그아웃을 붙여, 서버(RLS)가 "이 자료는 이 계정 것"을 강제하게 만든다. 그리고 "막았다"는 문장이 아니라 실제로 막히는 요청·응답 장면을 근거로 남긴다. (원문: [ASSINGMENT.md](ASSINGMENT.md))

옛 습관 기록기(카드1~5, localStorage)는 이번 과제 대상이 아니지만, 첫 화면이 로그인 화면이어야 한다는 요구(T07-C03) 때문에 같은 페이지 안에 있는 이상 로그인 게이트 뒤로 함께 숨긴다 — 기능·데이터는 건드리지 않는다.

## 지향점

- anon 롤에는 plans/todos/execution_logs/retrospective_notes/plan_revisions 어떤 표 권한도 없다 — "화면에서 안 보이게"가 아니라 서버가 애초에 안 준다
- 남의 행을 고치거나 지우려는 시도는 "조용히 0행"이 아니라 명시적인 거절 응답으로 남는다(가능한 한 403)
- 계정마다 같은 비밀번호를 써도 저장된 해시 값은 서로 다르다(bcrypt salt) — Supabase Auth에 위임해서 앱 코드가 직접 비밀번호를 만지지 않는다
- 로그아웃하거나 비밀번호를 바꾸면 이전 세션 값은 더는 통하지 않는다
- 계정 삭제는 내 자료 전체 삭제로 이어진다(FK on delete cascade) — "삭제될 거라는 안내"로 얼버무리지 않는다
- 6번(T06)에서 넣어 둔 실제 계획·할일·실행기록·돌아보기 메모는 지우지 않고 내 계정으로 그대로 옮긴다
- 옛 습관 기록기의 화면·기능·데이터(로컬 저장, 합성 데이터 원칙)는 건드리지 않고 그대로 유지한다
- ⑥ "아직 못 막은 것"을 비워 두지 않는다 — 실제로 안 한 것(무차별 대입 방지, 2단계 인증 등)은 있는 그대로 적는다

## 먼저 정할 것

- [x] 인증 방식을 정한다 → Supabase Auth(이메일+비밀번호). 이유: 이미 Supabase Postgres + RLS 위에서 동작 중이라 `auth.uid()`로 바로 연결되고, 비밀번호 해싱·세션 발급·재발급·만료를 직접 구현·검증할 필요가 없음
- [x] 소유권을 어느 레벨에서 강제할지 정한다 → 5개 표 전부에 직접 `user_id` 컬럼을 두고 RLS로 강제(조인으로 간접 확인하는 대신, 각 표가 스스로 주인을 안다 — 카드4 근거를 표 단위로 명확하게 만들기 위함)
- [x] 남의 행 UPDATE/DELETE 시도의 응답을 어떻게 만들지 정한다 → UPDATE는 `using(true)+with check`로 명시적 403(42501), DELETE/SELECT는 `using(user_id=auth.uid())`로 대상 자체를 안 보이게(사실상 빈 결과 — 카드4 근거에 실제 관측된 상태 코드를 그대로 적기로 함)
- [x] 기존 T06 실데이터를 어떻게 이관할지 정한다 → 컬럼을 nullable로 추가 → 내 실제 계정 생성 → UPDATE로 이관 → NOT NULL 잠금, 4단계. 데이터를 지우고 다시 넣지 않고 그대로 보존
- [x] 계정 삭제를 어떻게 구현할지 정한다 → `auth.users` DELETE는 기본적으로 service_role만 가능 → `auth.uid()`로 자기 자신만 지우도록 좁힌 `security definer` RPC(`delete_my_account`)로 셀프서비스 노출
- [ ] 이메일 확인(Confirm email) 정책을 켤지 끌지 정한다 → 사용자 확인 대기 중

## 최종 제출물

검증 안내서·AI 3줄·인증 구현 설명서는 이 문서 안에 먼저 초안을 쓰고, 제출 화면에는 그 섹션들만 옮겨 붙인다. 나머지(진행 상태 표, 카드별 상세, 마무리 체크리스트, 설계 이력, 부록 등)는 작업 기록용으로 이 문서에만 남는다.

### 완주 체크리스트

[ASSINGMENT.md](ASSINGMENT.md) "제출물" 섹션에 있는 7개 항목 원문 그대로. 카드별 세부 통과 기준(`T07-C##`)과는 별개로, 제출 전 마지막에 이 7개만 다시 확인한다. (진행 중 — 전부 미완료)

- [ ] 가입·로그인·로그아웃이 되고, 로그인 없이 자료 화면을 열면 로그인 화면이 나옵니다.
- [ ] 저장된 비밀번호에 입력한 글자가 보이지 않습니다.
- [ ] 로그아웃 뒤 같은 값으로 다시 요청하면 거절됩니다.
- [ ] 남의 자료를 읽기·수정·삭제하려는 요청이 양방향으로 모두 거절되고, 목록에도 섞이지 않습니다.
- [ ] 설명서 여섯 항목이 나뉘어 있고, ⑥이 비어 있지 않습니다.
- [ ] 서로 다른 날짜 5일의 기록과 3일차 전 규칙 변경이 순서대로 있습니다.
- [ ] 제출물에 비밀번호·토큰·비밀키 원문이 없습니다.

---

## 과제 카드별 진행 상황

카드별 원문(하는 일/완료 목표/첫 행동/통과 기준/막히는 지점/남길 것)은 [ASSINGMENT.md](ASSINGMENT.md) 참고. 여기서는 진행 상황과 남긴 결과물만 기록한다.

### 카드 1 — 무엇으로 붙일지 고르고, 이유를 적기 ⏳
Supabase Auth(이메일+비밀번호, `@supabase/supabase-js@2.112.4`) 채택. 검토했으나 고르지 않은 방법: 직접 구현(bcrypt+JWT), 별도 인증 서비스(Auth0/Clerk). 로그인/가입/로그아웃 화면(`index.html`의 `#auth-gate`, `pds-auth.js`)과 로그인 게이트(로그인 없이 자료 화면을 못 여는 것) 구현 완료. 라이브 Auth API로 가입 1차 확인 완료(#17). 남은 것: 실제 계정 생성 → 마이그레이션 STEP 4로 T06 자료 이관 → 로그인/로그아웃/중복가입/문구 동일성(T07-C99)까지 curl로 근거 수집.

### 카드 2 — 비밀번호를 어떻게 맡아 두는지 보이기 ⏳
비밀번호 저장은 전부 Supabase Auth(GoTrue)에 위임 — 앱 코드가 비밀번호를 직접 해싱하거나 저장하지 않는다. GoTrue는 내부적으로 bcrypt를 쓰는데, 이 프로젝트의 실제 저장 값(`auth.users.encrypted_password`)은 SQL Editor로만 조회 가능해 사용자 확인 대기 중(#22). 로그인 요청/응답에 비밀번호 원문이 안 남는지는 curl 테스트로 직접 확인 예정.

### 카드 3 — 들어온 사람을 어떻게 기억하는지 보이기 ⏳
Supabase Auth는 JWT 액세스 토큰(짧은 만료) + 리프레시 토큰 방식. 로그인 상태/로그아웃 후 같은 토큰으로의 요청을 나란히 비교하는 근거는 마이그레이션 적용 후 curl로 수집 예정. 비밀키(JWT signing secret)는 서버(Supabase)에만 있고 클라이언트 코드·`config.js`에는 anon key만 있음을 이미 확인(기존 T06 점검 유지, `pds-auth.js`도 새로 grep 재확인 필요).

### 카드 4 — 남의 자료가 안 열리는 것을 보이기 ⏳
RLS 설계 완료(#5~#8) — SELECT는 `user_id=auth.uid()`로 필터, UPDATE는 `using(true)+with check`로 명시적 403, INSERT는 소유자 위조를 막는 `with check`(부모 표 소유자 확인 포함). 실제 두 계정으로 교차 접근을 시도해 응답을 모으는 작업은 마이그레이션 적용 후 진행.

### 카드 5 — 설명서로 묶고, 잠근 앱으로 5일 써 보기 ⏳
아직 시작 전. 1일차 질문·지표·단위·계산 규칙을 먼저 고정하고, 마이그레이션 적용 후 실제 로그인 계정으로 5일에 걸쳐 기록한다.

---

## 마무리 체크리스트 (카드별 통과 기준 하나하나 대조)

`T07-C##` 세부 통과 기준을 하나씩 대조하는 작업용 상세 목록. 지금은 준비 단계라 대부분 미완료 — 진행되는 대로 갱신한다.

### 카드 1
- [x] T07-C91 인증을 무엇으로 붙였는지와 그 이름 — Supabase Auth
- [x] T07-C92 라이브러리/서비스 이름·버전 — `@supabase/supabase-js@2.112.4`
- [x] T07-C93 검토했으나 고르지 않은 방법과 이유 — "먼저 정할 것" 참고
- [x] T07-C94 가입 화면에서 새 계정 생성 — curl로 Auth API 직접 호출해 확인(`POST /auth/v1/signup` → `200`+세션 발급). 화면(UI) 클릭으로도 재확인 예정
- [x] T07-C95 만든 계정으로 로그인 — `POST /auth/v1/token?grant_type=password` → `200`+세션 발급 확인
- [x] T07-C96 로그아웃 — `POST /auth/v1/logout` → `204` 확인
- [x] T07-C97 로그인 없이 자료 화면 주소를 열면 로그인 화면 — `index.html`에 `hidden` 기본값으로 구현. anon key만으로 `/rest/v1/plans` 직접 요청 시 `401 permission denied`로 서버 단에서도 거절 확인
- [x] T07-C98 같은 아이디로 두 번 가입 안 됨 — 중복 이메일 재가입 시도 → `422 user_already_exists`
- [x] T07-C99 비밀번호 틀림/아이디 없음 안내 문구 동일 — 둘 다 `400 {"error_code":"invalid_credentials","msg":"Invalid login credentials"}`로 완전히 동일함을 실측 확인
- [ ] T07-C100 T06 자료가 내 계정으로 이관됨 — SQL로 `still_null=0`(계획 5건 전부 이관)까지 확인. 화면(UI)에서 실제로 보이는지 최종 재확인 남음

### 카드 2 ✅ (근거 수집 완료)
- [x] T07-C101 되돌릴 수 없게 만드는 방법 이름 — SQL Editor로 실제 저장 값 확인: `$2a$10$...` 형식 → **bcrypt**(cost factor 10) 확정
- [x] T07-C102 그 방법을 고른 이유 — "카드 1" 참고(직접 구현 대비 실수 위험 감소)
- [x] T07-C103 저장된 비밀번호 값(원문 안 보임) — 실제 계정의 `encrypted_password` 값 확보(`$2a$10$8tDv2...` 등, 입력한 글자와 전혀 무관한 형태)
- [x] T07-C104 같은 비밀번호 두 계정의 저장 값이 서로 다름 — 완전히 같은 비밀번호로 새 계정 두 개(X, Y) 생성 후 SQL Editor로 대조 → `$2a$10$2kJUjB6X...` vs `$2a$10$k3LicXws...`, salt가 달라 저장 값도 서로 다름을 확인
- [x] T07-C105 로그인 요청/응답 기록에 비밀번호 원문 없음 — 수집한 모든 응답 파일을 실제 비밀번호 문자열로 grep해 전무함을 확인
- [x] T07-C106 서버 로그·화면·네트워크 응답 어디에도 비밀번호 원문 없음 — 네트워크 응답은 위와 동일 확인. 화면·서버 로그는 최종 점검에서 재확인
- [x] T07-C107 비밀번호 처리를 라이브러리(Supabase Auth)에 맡겼음이 명시됨

테스트용으로 만든 계정 4개(A/B/X/Y)는 전부 `delete_my_account()`로 삭제 완료 — 지금 프로젝트에는 실제 계정 1개만 남아 있음(2026-09-01).

### 카드 3
- [x] T07-C108 세션/토큰 중 무엇인지 — JWT 액세스 토큰(`alg: ES256`, 비대칭키) + 리프레시 토큰(Supabase Auth)
- [x] T07-C109 로그인 상태 성공 응답 vs 로그아웃 후 거절 응답 나란히 — ⚠️ 실측 결과, access token은 로그아웃 후에도 자연 만료 전까지 계속 `200`(아래 ⑥ 참고). 대신 refresh token은 로그아웃 직후 `400 refresh_token_not_found`로 즉시 거절됨을 확인 — "거절 응답"의 실체는 refresh 시도 쪽
- [x] T07-C110 두 요청이 같은 주소·같은 방식 — `GET /rest/v1/plans?id=eq...`로 로그아웃 전/후 동일 요청 비교
- [x] T07-C111 만료 시각·끊기는 시간 — access token `expires_in=3600`(1시간), JWT payload의 `exp`/`iat` 값으로 직접 디코드해 확인
- [x] T07-C112 토큰이 URL에 안 실림 — 앱 코드(`pds-app.js`)와 curl 테스트 모두 `Authorization` 헤더로만 전달
- [x] T07-C113 JWT 비밀키가 브라우저 코드·배포 파일·Git 기록에 없음 — grep 전수 점검(아래 참고). 이 프로젝트는 서명 알고리즘이 `ES256`(비대칭키)라 애초에 "공유 비밀키" 자체가 없는 구조
- [ ] T07-C114 비밀번호 변경/로그아웃 후 이전 값 무효화 — 로그아웃 시 refresh token은 즉시 무효화되지만 access token은 그렇지 않음(⑥ 참고). 비밀번호 변경 케이스는 아직 미검증
- [x] T07-C115 기록에서 토큰 값 가림 — 이 문서·제출 문서에는 토큰 원문을 적지 않고 앞부분만 남기기로 원칙 확정(증거 보관 기준 참고)

### 카드 4
- [x] T07-C116 계정 두 개 + 각각 자료(비밀번호 미기재) — disposable 테스트 계정 A/B, 각각 계획 1건씩. 비밀번호는 이 문서 어디에도 없음
- [x] T07-C117~119 읽기/수정/삭제 거절(A→B) — 전부 `200`+빈 배열로 무효 처리, B의 자료 실제 값 대조로 변경 없음 확인
- [x] T07-C120 반대 방향도 거절(B→A) — 동일하게 확인
- [x] T07-C121 거절이 403 또는 404 — 실측 결과 경로별로 다름: 비로그인 직접 요청은 `401`(권한 자체 없음), 소유권 위조·교차 INSERT는 `403`(42501), 교차 UPDATE/DELETE는 `200`+빈 배열(값은 안 바뀜). 설계 의도(UPDATE도 명시 403)와 실측이 다른 이유는 정책 정의 재확인 중(다음 단계 참고)
- [x] T07-C122 거절 전후 건수 동일 — B 계정 자신의 토큰으로 재조회해 A의 시도 전후 제목·건수가 그대로임을 확인
- [x] T07-C123 주소/헤더/본문에 남의 계정 적어도 내 자료만 옴 — PATCH 본문에 남의 `user_id`를 넣어 소유권을 훔치려는 시도도 무효 처리됨을 확인
- [x] T07-C124 비로그인 직접 요청 거절 — `401 permission denied for table plans`(anon 권한 자체가 없음)
- [x] T07-C125 목록에 남의 자료 없음 — A의 목록 조회에 B의 자료 없음(1건, 본인 것만)
- [x] T07-C126 거절을 만드는 소스 위치 — `supabase/migration-t07-auth.sql` STEP 6(RLS 정책)·STEP 8(RPC 소유자 조건) / `supabase/schema.sql` 동일 섹션. 실제 거절 메커니즘의 정확한 서술은 정책 재확인 후 최종화

### 카드 5
- [ ] T07-C127~131 설명서 6항목·비밀값 가림
- [ ] T07-C04~C08 1일차 질문/지표/단위/계산 규칙 고정, 5일 기록
- [ ] T07-C23~C27 결측/중복/이상치/반올림/주 시작 요일 규칙
- [ ] T07-C09~C15 2일차 뒤·3일차 앞 규칙 변경, 전후 비교 일관성
- [ ] T07-C132 화면 합계와 손 계산 대조
- [ ] T07-C133 내보내기
- [x] T07-C134 계정 삭제 → 자료 삭제 — 테스트 계정 B로 `delete_my_account()` RPC 실행 → `204`. 이후 같은 이메일로 재로그인 시도 `400 invalid_credentials`(계정 소멸), 옛 계획도 더는 조회 안 됨(FK cascade로 실제 삭제) 확인
- [x] T07-C03 첫 화면이 로그인 화면(구조상 구현 완료), 로그인 뒤 자료 비공개 — 마이그레이션 적용 완료로 배포 반영만 남음
- [ ] T07-C39 짧은 확인 방법 4줄
- [ ] T07-C40 AI 3줄
- [x] T07-C46 비밀값 원문 없음(전체 재점검) — grep 전수 점검 완료(아래 참고)
- [ ] T07-C01 새 시크릿 창에서 계정 없이 열림 — 배포 반영 후 재확인
- [ ] T07-C77/C78 T06 최종 결과 이어받음 + T06 커밋이 조상으로 포함 — 이미 `archive/t06/`가 이번 브랜치 이력 위에 그대로 있어 조상 관계는 자동 성립, 문서에 명시만 하면 됨

---

## 검증 안내서

*(마이그레이션 적용·실사용 로그 완료 후 작성 — 초안 자리만 남겨 둠)*

① 어디로 가나요 —
② 세 단계 안에 무엇을 하나요 —
③ 무엇이 보이면 통과인가요 —
④ 안 될 때는 무엇이 보이나요 —

## 인증 구현 설명서

*(카드5에서 최종 작성 — 아래는 지금까지 확정된 내용만 미리 채워 둔 초안)*

① 무엇으로 붙였나 — Supabase Auth(이메일+비밀번호), `@supabase/supabase-js@2.112.4`.

② 왜 그걸 골랐나 — 이미 Supabase Postgres + RLS 위에서 이 앱이 동작 중이라 `auth.uid()`로 바로 연결됨. 검토했으나 고르지 않은 방법: 직접 구현(bcrypt+JWT) — 세션 무효화·토큰 재발급 등 보안 디테일을 직접 구현·검증하는 부담이 큼. 별도 인증 서비스(Auth0/Clerk) — 인프라가 하나 더 늘고 DB RLS와의 연동 지점이 늘어남.

③ 어디를 어떻게 고쳤나 — *(마이그레이션 적용 후 실제 파일·라인 단위로 정리 예정)* 지금까지 변경한 파일: `supabase/migration-t07-auth.sql`(신규, RLS·소유권 전면 교체), `supabase/schema.sql`(신규 설치 기준 갱신), `contracts/pds-schema-v2.json`(스키마 계약 갱신), `index.html`(`#auth-gate` 신설, 로그인 게이트), `pds-auth.js`(신규, 로그인 게이트 로직), `pds-app.js`(`Auth` 래퍼 추가, 클라이언트 인스턴스 공유), `pds-ui.js`(자동 초기화를 로그인 이후로 지연), `style.css`(로그인 화면 스타일).

④ 안 열리는 것을 확인한 기록 — curl 테스트 결과 요약(위 진행 상태 표 아래 섹션) 참고. 최종 문구는 실제 요청/응답 캡처와 함께 카드5에서 정리.

⑤ AI와 나 — *(카드5에서 "AI와 내 판단 3줄"과 함께 작성)*

⑥ 아직 못 막은 것 — *(진행하며 드러나는 대로 갱신. 2026-09-01 실측으로 확정된 것)*
- **로그아웃(또는 계정 삭제) 후에도 이미 발급된 access token은 자연 만료(최대 1시간) 전까지 계속 통한다.** `POST /auth/v1/logout`은 refresh token만 즉시 무효화하고, JWT access token은 서명·만료(exp)만으로 자체 검증되는 무상태(stateless) 토큰이라 서버가 개별적으로 "이 토큰은 취소됨"을 기억하지 않는다. 위험: 누군가 access token을 탈취한 뒤 피해자가 로그아웃해도, 최대 1시간 동안은 그 토큰으로 계속 자료에 접근할 수 있다. 완화책(짧은 만료 시간, 1시간)은 있지만 즉시 차단은 아직 못 함 — 서버 쪽 토큰 블랙리스트/세션 버전 관리를 붙이지 않았기 때문.
- **남의 계획을 고치거나 지우려는 시도가 "조용한 무효 처리"(200+빈 배열)로 응답하고, 명시적 403이 아니다** — 원인은 SQL Editor로 실제 정책 정의(`plans_update`: `qual=true, with_check=(user_id=auth.uid())`)를 확인해 규명함: Postgres RLS는 UPDATE/DELETE 대상 행을 찾을 때 그 표의 SELECT 정책도 함께 적용한다 — `using(true)`를 걸어도, 애초에 SELECT 정책을 통과 못 하는 남의 행은 UPDATE 후보 자체로 안 보여 0건 처리된다(존재 자체를 감추는 쪽에 더 가까운 동작). 반면 **내 소유 행**의 소유권을 남에게 넘기려는 시도는 SELECT 정책은 통과하므로 행이 보이고, 그다음 `with_check`가 걸려 명시적 403이 난다. 데이터는 두 경우 모두 안전하지만, 의도한 "항상 뚜렷한 403"은 아니고 케이스에 따라 다르다 — 이것 자체를 "안 열리는 것을 확인한 기록"에 정직하게 남긴다.
- 무차별 대입(brute-force) 로그인 시도 횟수 제한을 이 앱이 직접 만들지는 않음 — Supabase Auth 자체의 기본 rate limit에 의존.

## AI와 내 판단 3줄

*(카드5에서 작성)*

## 증거 보관 기준

- 제출 화면에는 "검증 안내서"·"인증 구현 설명서"·"AI와 내 판단 3줄" 섹션의 내용만 옮겨 붙인다.
- 요청/응답 기록에 비밀번호·토큰·비밀키 원문을 남기지 않는다 — 토큰은 앞부분만 남기고 `eyJhb…생략`처럼 자른다.
- 두 계정 간 자료 격리 테스트에 쓴 계정의 비밀번호는 어디에도 적지 않는다.
- 습관 기록기 쪽은 옛 원칙대로 합성 데이터만 유지한다.
- 그 외 근거 자료(SQL 마이그레이션 원본, 설계 결정 과정 등)는 이 문서에만 남기고 제출 화면에는 올리지 않는다.

---

## 설계 결정 이력

> 필드·스키마·보안·인증 방식처럼 근거로 남겨야 할 결정만 기록. T06의 설계 이력은 [archive/t06/PROGRESS.md](archive/t06/PROGRESS.md)에 보존돼 있다.

- 2026-09-01: 인증 방식으로 Supabase Auth(이메일+비밀번호) 채택. 대안으로 직접 구현(bcrypt+JWT)과 별도 인증 서비스(Auth0/Clerk)를 검토했으나, 이미 Supabase Postgres 위에서 RLS로 소유권을 통제하고 있어 `auth.uid()`와 바로 연결되는 Supabase Auth가 가장 적은 위험으로 이어 붙일 수 있는 선택.
- 2026-09-01: 소유권 컬럼(`user_id`)을 5개 표 전부에 직접 둠(조인으로 간접 확인하지 않음) — 표마다 스스로 주인을 알아야 카드4가 요구하는 "그 거절을 만드는 소스 위치"를 표 단위로 명확히 짚을 수 있음.
- 2026-09-01: 남의 행에 대한 UPDATE 시도가 "조용히 0행"으로 끝나지 않도록 `using(true) + with check(user_id=auth.uid())` 조합을 선택 — RLS의 `using`만 좁게 걸면(행이 안 보이면) 0행 갱신으로 200/204가 나서 "거절 장면"이 뚜렷하게 안 남는다고 판단. 대신 이 선택은 "행이 존재하는지"를 403 여부로 흘릴 수 있는 부작용이 있어(존재 확인/enumeration), 카드5 ⑥"아직 못 막은 것" 후보로 미리 기록해 둠.
- 2026-09-01: 기존 T06 실데이터는 새로 넣지 않고 그대로 이관하기로 결정 — 컬럼을 nullable로 추가 → 실제 계정 생성 → UPDATE로 이관 → NOT NULL 잠금 순서로 마이그레이션을 설계해, 서비스 중단 없이(짧은 접근 불가 구간 제외) 데이터를 보존.
- 2026-09-01: 계정 삭제(`delete_my_account`)는 `auth.users` 삭제 권한이 기본적으로 `service_role` 전용이라는 제약 아래, `auth.uid()`로 자기 자신만 지우도록 좁힌 `security definer` 함수로 셀프서비스를 열기로 결정 — 클라이언트에 `service_role` 키를 두는 것보다 안전.
- 2026-09-01: `pds-app.js`와 `pds-auth.js`가 별도로 `createClient()`를 호출하면 로그인 상태가 서로 동기화되지 않는 문제를 발견 — `window.PDS_SB`로 인스턴스를 하나만 만들어 공유하도록 변경.

---

## 부록: 파일 구조

```
/
├── ASSINGMENT.md                  # 동결된 요구사항 (T07)
├── PROGRESS.md                    # 이 문서
├── README.md                      # T07 진행 중 상태 반영
├── archive/t06/                   # T06 문서 전체(ASSINGMENT/PROGRESS/SUBMISSION/card5-log/submission) 보존
├── index.html                     # #auth-gate(로그인 게이트, 첫 화면) + 습관 기록기 + Plan-Do-See
├── style.css                      # 옛 다크 테마 + 로그인 화면 스타일(같은 변수 재사용)
├── app.js / ui.js / checklist.js  # 옛 습관 기록기(로컬 저장, 변경 없음)
├── config.js                      # Supabase URL + anon key (공개 키, 변경 없음)
├── pds-app.js                     # 데이터 계층 + Auth 래퍼(신규), window.PDS_SB 클라이언트 공유
├── pds-ui.js                      # 렌더링 + 이벤트, 로그인 확인 전엔 자동 초기화 안 함(변경)
├── pds-auth.js                    # 신규 — 로그인 게이트 로직
├── supabase/
│   ├── schema.sql                 # DDL(신규 설치 기준, user_id·RLS 포함)
│   └── migration-t07-auth.sql     # 신규 — 기존 T06 데이터 보존 마이그레이션(ALTER 기반)
├── contracts/
│   └── pds-schema-v2.json         # 스키마 계약(user_id·보안 섹션 갱신)
├── fixtures/v1-sample.json        # 옛 습관 기록기 v1 샘플(변경 없음)
└── submission/                    # T07용 제출 증거(추후 작성)
```

## 부록: 시간대·식별자 규칙

- 기준 시간대: `Asia/Seoul` (T06과 동일, 고정)
- 날짜 전용 컬럼: 시간 없는 `date`, Asia/Seoul 달력 기준
- 시각 컬럼: UTC로 저장(`timestamptz`), 화면은 Asia/Seoul로 변환 표시
- 고유 ID: Postgres `gen_random_uuid()` (계정 식별자는 `auth.users.id`, uuid)

## 부록: 열린 질문 / 리스크

- 이메일 확인(Confirm email) 정책을 켜 둘지 끌지 — 사용자 확인 대기 중. 켜 두면 카드2~4의 curl 테스트마다 이메일 확인 절차가 끼어들어 자동화가 번거로워짐.
- UPDATE 정책의 `using(true)` 설계가 만드는 "존재 확인(enumeration)" 여지 — 카드5 ⑥에 정직하게 남길 후보.
- 카드5(5일 실사용)는 사용자가 실제 날짜에 걸쳐 직접 써야 하는 부분이라 AI가 하루 만에 대신 채울 수 없다 — 마이그레이션 적용 이후 순차 진행.
- 옛 습관 기록기와 Plan-Do-See가 한 페이지에 있고, 이번엔 그 페이지 전체가 로그인 게이트 뒤에 있음 — 검증 안내서 작성 시 "로그인 후 기본 화면이 Plan-Do-See"라는 점을 분명히 적어야 함.
