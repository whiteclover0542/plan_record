# T06 플랜두씨 다이어리 1

> 이 문서는 과제 요구사항 원본이다. 작성 후 요구사항을 수정하지 않는다.

## 과제 개요

- 주제: 계획한 나와 실제의 나를 비교하는 개인 기록기
- 난이도: 계획시간 5~6시간
- 별도 경과 기간: 5일(작업시간 합계 제외)
- 과제 카드: 5개
- 정렬 순서: 6
- 공개 상태: 공개 / 열림

습관, 운동, 게임 기록, 학습 시간 등 실제로 남기고 싶은 항목을 기록·수정·삭제하고 다시 불러오는 개인 기록기를 만든다.

## 완료 기준

- 공개 주소에서 가상 기록을 추가·수정·삭제할 수 있다.
- 기록을 바꾸면 목록과 요약값이 함께 바뀐다.
- 내보내기·가져오기·초기화 기능이 화면에 보인다.
- 실제 개인 기록과 개인정보가 보이지 않는다.

## 강사 판정 기준

학생이 요구사항 카드 5개를 순서대로 읽을 수 있어야 한다. 통과 판단은 기존 제출·강사 확인·오너 최종 인정 계약을 따른다.

## 과제 카드

### 카드 1 — 기록의 구조

- 목표: 기록 한 건의 필드와 단위를 정하고 생성·조회·수정·삭제가 모두 가능한 첫 버전을 만든다.
- 요구사항: 항목, 단위, 날짜, 기준 시간대를 정하고 기록의 생성·조회·수정·삭제를 구현한다.
- 통과 기준: 공개 화면에 가상 기록의 추가·수정·삭제 항목과 단위·기준 시간대가 보인다.
- 시작 작업:
  1. 무엇을, 어떤 숫자나 상태로, 어느 날짜와 시간대에 기록할지 한 문장으로 정한다.
  2. 고유 ID·날짜·시간대·항목·값·단위·메모 표를 만든다.
  3. 합성 기록 3건으로 추가·보기·수정·삭제를 구현한다.

### 카드 2 — 내보내기와 전체 삭제

- 목표: 새로고침 뒤에도 기록을 유지하고 내보내기·복원·전체 삭제가 가능하게 만든다.
- 요구사항: JSON 또는 CSV로 내보내기·복원하고, 사용자가 전체 기록을 삭제할 수 있어야 한다.
- 통과 기준: 공개 화면에 내보내기·가져오기·전체 삭제 기능과 오류 이유를 보여주는 영역이 보인다.
- 시작 작업:
  1. 합성 기록 5건을 저장하고 새로고침 전·후 ID·날짜·값을 대조한다.
  2. JSON 또는 CSV 하나를 정해 내보낸다.
  3. 시험 상태에서 복원, 전체 삭제, 손상 파일 처리를 검사한다.

### 카드 3 — 기존 기록을 잃지 않는 변경

- 목표: 새 필드를 추가하면서 이전 형식의 기록을 자동 변환하고 기존 값을 보존한다.
- 요구사항: 태그나 단위 등 새 필드와 `schemaVersion`을 추가하고 v1 기록을 v2로 변환한다.
- 통과 기준: 현재 자료 형식과 변환 상태가 보이며, 변환 뒤에도 같은 가상 기록을 확인할 수 있다.
- 시작 작업:
  1. 카드 2 형식의 v1 합성 기록 파일을 보관한다.
  2. 새 필드와 `schemaVersion`을 추가한다.
  3. v1을 읽으면 v2로 한 번만 변환하는 함수를 만든다.

### 카드 4 — 날짜와 잘못된 값

- 목표: 주 경계와 잘못된 입력을 포함한 합성 자료로 집계가 흔들리지 않게 만든다.
- 요구사항: 월요일·일요일 경계, 누락값, 중복값, 잘못된 날짜를 검사한다.
- 통과 기준: 주간 요약의 기간과 집계값이 보이며 잘못된 날짜나 값은 집계에 섞이지 않는다.
- 시작 작업:
  1. 월요일 00:00·일요일 23:59·누락값·중복 ID·잘못된 날짜·숫자가 아닌 값을 포함한 자료를 만든다.
  2. 각 입력을 저장·거부·별도 표시 중 어떻게 처리할지 예상 결과표를 먼저 만든다.

### 카드 5 — 5일 사용 후 한 가지 개선

- 목표: 서로 다른 실제 날짜 5일에 사용하고 반복해서 불편했던 한 가지를 고친다.
- 요구사항: 잘못 입력한 기록 1건을 바로잡고 요약값도 함께 바뀌는지 확인한다.
- 통과 기준: 가상 기록을 고치면 해당 행과 관련 요약값이 같은 화면에서 함께 바뀐다.
- 시작 작업:
  1. 카드 1을 마친 날부터 매일 실제 기록을 PC에 1건 이상 남긴다.
  2. 다섯째 날짜에 반복된 불편 하나를 고친다.
  3. 잘못 입력한 기록 1건과 관련 요약값을 다시 계산한다.

## 필수 검증·보안 계약

- 고유 ID를 사용하며 수정·삭제는 정확한 한 건에만 반영한다.
- 필수값이 빈 기록은 저장하지 않는다.
- 새로고침 뒤 기록 수와 값이 유지된다.
- 내보낸 파일에서 ID·날짜·값·단위가 복원된다.
- 전체 삭제 뒤 기록은 0건이다.
- 손상 파일은 기존 기록을 바꾸지 않고 오류 이유를 표시한다.
- v1·v2 변환 뒤 건수와 기존 값이 유지된다.
- 변환을 두 번 실행해도 기록이 늘거나 다시 바뀌지 않는다.
- 기준 시간대와 주 시작 요일을 명확히 한다.
- 날짜 경계·누락·중복·잘못된 값은 정한 규칙대로 처리하며 유효하지 않은 값은 집계에 섞지 않는다.
- 실제 개인 기록은 PC에만 두고 공개·제출에는 합성 데이터 또는 가림 처리한 집계값만 사용한다.
- 개인정보와 비밀값은 공개 화면·파일·제출 기록에 0건이어야 한다.
- 5일의 기다린 시간은 능동 작업시간 5~6시간에 포함하지 않는다.

## 제출물 계약

- 검증 안내서 1개: `어디로 가나요` / `무엇을 하나요` / `무엇이 보이면 통과인가요` / `안 될 때`
- AI 3줄: AI에게 맡긴 일 · 내가 판단한 일 · AI 말을 안 들은 일
- 검증 안내서는 공개 주소에서 3단계 이내 행동으로 30초 안에 확인할 수 있게 작성한다.
- 오너는 공개 주소의 현재 결과를 15초 안에 판정할 수 있어야 한다.
- AI가 작성한 한국어는 번역투와 존댓말 혼용이 없도록 직접 읽고 다듬는다.
